# 2026-07-02 Patch Notes

## Chrome Extension Addon And Developer Handoff Docs

Time: 09:04 KST
Commit: `08e72f8`
Status: released

### Summary

- Added a Manifest V3 Chrome extension for submitting typed URLs or the current tab URL to HugCivi.
- Added the web UI `애드온` button and `/api/addon/chrome-extension` zip endpoint.
- Added developer handoff references: document index, feature-code map, configuration reference, and transfer design.
- Updated current docs and marked dated planning/review docs as historical where needed.

### Code And Docs

- Backend: `app/main.py`, `Dockerfile`
- Frontend/addon: `app/templates/index.html`, `app/static/style.css`, `chrome-extension/`
- Tests: `tests/test_review_fixes.py`
- Docs: `README.md`, `PATCH_NOTES.md`, `docs/index.md`, `docs/feature-code-map.md`, `docs/configuration.md`, current reference docs

### Verification

- `git diff --check`: passed
- Python compile for core modules: passed
- Chrome extension JS and manifest checks: passed
- `python3 -m pytest -q -p no:cacheprovider`: `127 passed`
- Local Markdown link check: passed

### Deploy Impact

- Built and pushed `ghcr.io/mephiblin/hugcivi:sha-08e72f8`.
- Updated `ghcr.io/mephiblin/hugcivi:latest`.
- Pushed `08e72f8` to `origin/main`.
- Portainer can pull `ghcr.io/mephiblin/hugcivi:latest`.

### Handoff Notes

- The Docker image must include `chrome-extension/`; `Dockerfile` copies it explicitly.
- The addon endpoint packages a loadable `hugcivi-chrome-extension/` folder.
- `docs/feature-code-map.md` is the first stop for future code changes.

### Follow-Up

- None for the released addon/docs change.

## Repo-Local Skill Set And LLM Documentation Structure

Time: 09:22 KST
Commit: `pending`
Status: verified

### Summary

- Added `SKILL_Dev/` as a repo-local skill set for stable HugCivi development workflows.
- Added `README_LLM.md` as the LLM/developer entry point.
- Added `AGENTS.md` for short Codex project guidance that is loaded automatically.
- Added `.agents/skills/hugcivi-dev-core/` as a Codex auto-discovery pointer into the repo-local skill set.
- Added this `docs/patch-notes/` folder and guide for date-based work records.

### Code And Docs

- Docs: `AGENTS.md`, `README_LLM.md`, `.agents/skills/hugcivi-dev-core/`, `SKILL_Dev/`, `docs/patch-notes/`, `README.md`, `PATCH_NOTES.md`, `docs/index.md`, `docs/feature-code-map.md`

### Verification

- `git diff --check`: passed
- Local Markdown link check: passed
- Skill frontmatter check: passed

### Deploy Impact

- None unless this docs change is included in a release/build push.

### Handoff Notes

- Codex documentation recommends `AGENTS.md` for small persistent repo rules and `.agents/skills` for discoverable reusable workflows. HugCivi keeps detailed browsable repo-local skills in `SKILL_Dev/` and exposes them through `.agents/skills/hugcivi-dev-core/`.

### Follow-Up

- Optional later cleanup: split each focused `SKILL_Dev/skill_*.md` into its own `.agents/skills/<name>/SKILL.md` folder if implicit skill routing needs to be more granular.

## YouTube Channel And Playlist Folder Routing

Time: 14:29 KST
Commit: `pending`
Status: verified

### Summary

- Changed YouTube yt-dlp archive routing so playlist URLs save under `/data/gallery-dl/youtube.com/playlist/<playlist-id>`.
- Changed non-playlist YouTube URLs to save under `/data/gallery-dl/youtube.com/channel/<channel-name>` when yt-dlp metadata exposes a channel or uploader name.
- Preserved the old URL-derived folder fallback when channel metadata is unavailable.

### Code And Docs

- Backend: `app/downloader.py`
- Frontend/addon: None
- Tests: `tests/test_downloader_runtime.py`
- Docs: `README.md`, `docs/configuration.md`, `docs/feature-code-map.md`

### Verification

- `python3 -m py_compile app/downloader.py app/parsers.py app/models.py`: passed
- `python3 -m pytest -q -p no:cacheprovider tests/test_youtube_parser.py tests/test_downloader_runtime.py`: `68 passed`
- `git diff --check`: passed
- `python3 -m pytest -q -p no:cacheprovider tests/test_bulk_add.py tests/test_civitai_image_parser.py tests/test_hitomi_listing.py tests/test_youtube_parser.py tests/test_main_urls.py tests/test_downloader_runtime.py`: `86 passed`
- `python3 -m pytest -q -p no:cacheprovider`: `129 passed`

### Deploy Impact

- Existing YouTube archives are not moved automatically. New YouTube downloads use the new folder layout after deploy.

### Handoff Notes

- `YT_DLP_METADATA_PROBE_TIMEOUT_SECONDS` controls the pre-download metadata probe used for channel folder naming.
- Playlist URLs are identified from the URL `list=` parameter before probing, so they avoid the extra channel metadata probe.

### Follow-Up

- None planned.

## YouTube Subscription Design Documentation

Time: 14:50 KST
Commit: `pending`
Status: verified

### Summary

- Documented a future YouTube subscription system design inspired by Pinchflat while keeping HugCivi's one-shot download queue separate.
- Captured the proposed left-sidebar subscription tab, add-subscription modal, initial download policies, independent subscription scheduler, conservative queue defaults, DB tables, and API surface.
- Marked the design as not implemented so it is not confused with current behavior.

### Code And Docs

- Backend: None
- Frontend/addon: None
- Tests: None
- Docs: `docs/youtube-subscriptions-design-2026-07-02.md`, `docs/index.md`

### Verification

- `git diff --check`: passed
- Python compile for core modules: passed
- Chrome extension JS and manifest checks: passed
- `rg -n "youtube-subscriptions-design-2026-07-02|YouTube Subscription Design|YT_DLP_METADATA_PROBE_TIMEOUT_SECONDS" README.md docs PATCH_NOTES.md SKILL_Dev`: found expected docs references
- `python3 -m pytest -q -p no:cacheprovider`: `129 passed`

### Deploy Impact

- None. Documentation only.

### Handoff Notes

- Current code still supports one-shot YouTube/yt-dlp downloads only. Subscription discovery, subscription items, and subscription workers are future work.
- The design recommends independent subscription state and UI while reusing existing yt-dlp configuration and archive path helpers where practical.

### Follow-Up

- Implement Phase 1 DB migrations and helper tests when ready to start the feature.

## YouTube Subscription Implementation Plan Refinement

Time: 14:56 KST
Commit: `pending`
Status: verified

### Summary

- Refined the YouTube subscription design into a more implementation-ready plan.
- Added explicit decisions for a dedicated `app/subscriptions.py` module, separate subscription tables, independent schedulers, subscription item state machines, backend settings, restart behavior, and test coverage.
- Clarified that subscription downloads should not call `download_gallerydl(job_id, parsed)` directly because that path assumes the visible `jobs` table.

### Code And Docs

- Backend: None
- Frontend/addon: None
- Tests: None
- Docs: `docs/youtube-subscriptions-design-2026-07-02.md`

### Verification

- `git diff --check`: passed
- `rg -n "app/subscriptions.py|State Machines|SUBSCRIPTION_CHECK_MAX_CONCURRENT|Implementation Plan Refinement|download_gallerydl\\(job_id" docs/youtube-subscriptions-design-2026-07-02.md docs/patch-notes/2026-07-02.md`: found expected implementation-plan anchors

### Deploy Impact

- None. Documentation only.

### Handoff Notes

- The recommended implementation path is now: schema/API discovery first, sidebar UI next, scheduled checks after that, and independent subscription downloads last.
- Refactor shared yt-dlp target/command/process helpers before implementing automatic subscription downloads.

### Follow-Up

- Start Phase 1 with additive DB migrations and DB helper tests.

## YouTube Subscription Phase 1 Data Model

Time: 15:07 KST
Commit: `pending`
Status: verified

### Summary

- Added the first implementation slice for YouTube subscriptions.
- Created additive SQLite tables and indexes for `subscriptions` and `subscription_items`.
- Added subscription DB helpers, payload/default helpers, and read-only API endpoints for subscription state.
- Kept schedulers, yt-dlp discovery, subscription creation, UI, and automatic downloads disabled for later phases.

### Code And Docs

- Backend: `app/db.py`, `app/subscriptions.py`, `app/main.py`
- Frontend/addon: None
- Tests: `tests/test_youtube_subscriptions.py`
- Docs: `docs/youtube-subscriptions-design-2026-07-02.md`, `docs/index.md`, `docs/feature-code-map.md`, `docs/architecture.md`

### Verification

- `python3 -m py_compile app/main.py app/db.py app/subscriptions.py app/downloader.py app/internal_jobs.py app/parsers.py app/workflows.py app/metadata.py app/utils.py`: passed
- `python3 -m pytest -q -p no:cacheprovider tests/test_youtube_subscriptions.py`: `5 passed`
- `git diff --check`: passed
- `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py tests/test_downloader_runtime.py tests/test_youtube_subscriptions.py`: `100 passed`
- `python3 -m pytest -q -p no:cacheprovider`: `134 passed`

### Deploy Impact

- Adds two SQLite tables and indexes on startup through additive migrations. No background subscription work starts after deploy.

### Handoff Notes

- `/api/subscriptions`, `/api/subscriptions/{id}`, and `/api/subscriptions/{id}/items` are read-only Phase 1 APIs.
- `app/subscriptions.py` intentionally reports disabled schedulers until future phases add discovery and workers.

### Follow-Up

- Phase 2: implement create/list/update/delete subscription APIs and manual `check now` discovery with mocked yt-dlp tests.

## YouTube Subscription Phase 2 Manual Discovery

Time: 15:16 KST
Commit: `pending`
Status: verified

### Summary

- Added backend create, update, delete, and manual check APIs for YouTube subscriptions.
- Added YouTube channel/playlist source validation, initial policy normalization, interval validation, and enabled/paused state handling.
- Added manual yt-dlp flat metadata discovery that stores `subscription_items` without creating normal `jobs` rows or downloading media.
- Added discovery failure backoff state and redacted API/DB error reporting for sensitive token-like values.

### Code And Docs

- Backend: `app/subscriptions.py`, `app/main.py`, `app/db.py`
- Frontend/addon: None
- Tests: `tests/test_youtube_subscriptions.py`
- Docs: `README.md`, `docs/youtube-subscriptions-design-2026-07-02.md`, `docs/index.md`, `docs/feature-code-map.md`, `docs/architecture.md`, `docs/configuration.md`, `docs/operations.md`, `PATCH_NOTES.md`

### Verification

- `python3 -m py_compile app/main.py app/db.py app/subscriptions.py`: passed
- `python3 -m pytest -q -p no:cacheprovider tests/test_youtube_subscriptions.py`: `8 passed`
- `git diff --check`: passed

### Deploy Impact

- Existing installs gain mutation APIs for subscription rows and a manual discovery path. No subscription scheduler or automatic download worker starts after deploy.

### Handoff Notes

- `POST /api/subscriptions/{id}/check` invokes yt-dlp metadata discovery synchronously for now, then writes discovered videos into `subscription_items`.
- Manual discovery intentionally does not create rows in `jobs`; visible one-shot downloads remain separate from subscription state.
- `/api/subscriptions` reports disabled schedulers with phase `manual_discovery`.

### Follow-Up

- Phase 3: add the left-sidebar subscription tab, add-subscription modal, subscription list, and manual check button.

## YouTube Subscription Phase 3 Sidebar UI

Time: 15:22 KST
Commit: `pending`
Status: verified

### Summary

- Added the left-sidebar `저장 폴더` / `구독` segmented tabs.
- Added the subscription list panel with state, interval, last/next check metadata, item counters, manual check, pause/resume, and delete controls.
- Added the YouTube subscription modal with URL, initial policy, latest-N count, full-backfill confirmation, interval presets, enabled toggle, and auto-queue toggle.
- Connected the UI to the subscription CRUD and manual check APIs while keeping subscription activity out of the normal job list.

### Code And Docs

- Backend: `app/main.py`, `app/subscriptions.py`
- Frontend/addon: `app/templates/index.html`, `app/static/style.css`
- Tests: `tests/test_review_fixes.py`, `tests/test_youtube_subscriptions.py`
- Docs: `docs/youtube-subscriptions-design-2026-07-02.md`, `docs/index.md`, `docs/feature-code-map.md`, `PATCH_NOTES.md`

### Verification

- `python3 -m pytest -q -p no:cacheprovider tests/test_youtube_subscriptions.py tests/test_review_fixes.py::test_home_template_declares_subscription_sidebar_ui`: `9 passed`
- Inline `app/templates/index.html` script syntax check with Node after replacing Jinja JSON constants: passed
- `git diff --check`: passed

### Deploy Impact

- The web UI now exposes the manual subscription management surface. No background scheduler or automatic subscription download worker starts after deploy.

### Handoff Notes

- The subscription UI polls `/api/subscriptions` and posts manual checks to `/api/subscriptions/{id}/check`.
- The current `자동 대기열` toggle is stored with the subscription for future workers; current manual discovery still does not enqueue download jobs.

### Follow-Up

- Phase 4: add the independent subscription check scheduler with jitter, backoff scheduling, and restart handling.

## YouTube Subscription Phase 4 Check Scheduler

Time: 15:27 KST
Commit: `pending`
Status: verified

### Summary

- Added an independent subscription check scheduler thread in `app/subscriptions.py`.
- Registered subscription scheduler start/stop in FastAPI lifespan separately from the normal download scheduler and internal job scheduler.
- Added due-subscription DB helpers and restart recovery for subscriptions left in `checking`.
- Added startup jitter, poll interval, success next-check scheduling, failure backoff scheduling, and in-process duplicate-check protection.
- Kept subscription downloads disabled; the scheduler only discovers items into `subscription_items`.

### Code And Docs

- Backend: `app/subscriptions.py`, `app/db.py`, `app/main.py`
- Frontend/addon: `app/templates/index.html`
- Tests: `tests/test_youtube_subscriptions.py`, `tests/test_review_fixes.py`
- Docs: `README.md`, `docs/youtube-subscriptions-design-2026-07-02.md`, `docs/index.md`, `docs/feature-code-map.md`, `docs/architecture.md`, `docs/configuration.md`, `docs/operations.md`, `PATCH_NOTES.md`

### Verification

- `python3 -m py_compile app/main.py app/db.py app/subscriptions.py`: passed
- `python3 -m pytest -q -p no:cacheprovider tests/test_youtube_subscriptions.py`: `9 passed`
- `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py::test_lifespan_runs_startup_tasks_and_stops_workers tests/test_review_fixes.py::test_home_template_declares_subscription_sidebar_ui`: `2 passed`
- `git diff --check`: passed

### Deploy Impact

- Enabled subscriptions are now checked by HugCivi itself after deploy. Checks use yt-dlp flat metadata discovery and do not create normal download jobs.

### Handoff Notes

- `SUBSCRIPTION_CHECK_SCHEDULER_ENABLED=0` disables the check scheduler if an operator wants manual-only behavior.
- `SUBSCRIPTION_CHECK_POLL_SECONDS`, `SUBSCRIPTION_STARTUP_JITTER_MIN_SECONDS`, and `SUBSCRIPTION_STARTUP_JITTER_MAX_SECONDS` tune scheduler wake behavior.
- Manual and scheduled checks share duplicate-check protection through `SubscriptionCheckAlreadyRunning`.

### Follow-Up

- Phase 5: refactor shared yt-dlp helpers and add the independent subscription download worker.

## YouTube Subscription Phase 5 Download Worker

Time: 15:35 KST
Commit: `pending`
Status: verified

### Summary

- Added an independent subscription download worker that consumes eligible `subscription_items`.
- Reused existing yt-dlp command construction and YouTube archive target routing so subscription files land under the same playlist/channel layout.
- Added `subscription_items.log` plus progress, filename, target path, attempt, error, done, and retry-backoff updates.
- Added DB helpers for ready subscription items, interrupted download recovery, item updates, and item log appends.
- Kept subscription downloads separate from normal `jobs`; no visible one-shot job rows are created.

### Code And Docs

- Backend: `app/subscriptions.py`, `app/db.py`, `app/main.py`
- Frontend/addon: `app/templates/index.html`
- Tests: `tests/test_youtube_subscriptions.py`, `tests/test_review_fixes.py`
- Docs: `README.md`, `docs/youtube-subscriptions-design-2026-07-02.md`, `docs/index.md`, `docs/feature-code-map.md`, `docs/architecture.md`, `docs/configuration.md`, `docs/operations.md`, `PATCH_NOTES.md`

### Verification

- `python3 -m py_compile app/main.py app/db.py app/subscriptions.py app/downloader.py`: passed
- `python3 -m pytest -q -p no:cacheprovider tests/test_youtube_subscriptions.py`: `11 passed`
- `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py::test_lifespan_runs_startup_tasks_and_stops_workers tests/test_review_fixes.py::test_home_template_declares_subscription_sidebar_ui`: `2 passed`
- `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py tests/test_downloader_runtime.py tests/test_youtube_subscriptions.py`: `107 passed`
- `python3 -m pytest -q -p no:cacheprovider`: `141 passed`
- Chrome extension JS and manifest checks: passed
- Dev server smoke on `127.0.0.1:8091` with temp `/data` and `/config`: home HTML contained subscription tab/modal anchors; `/api/subscriptions` returned running check/download schedulers.
- `git diff --check`: passed

### Deploy Impact

- Enabled subscriptions with `auto_queue` on can now download eligible discovered videos automatically through the subscription worker.
- The worker uses the same `/data/gallery-dl/youtube.com/playlist/<id>` and `/data/gallery-dl/youtube.com/channel/<name>` archive layout.

### Handoff Notes

- `SUBSCRIPTION_DOWNLOAD_SCHEDULER_ENABLED=0` keeps discovery enabled while disabling automatic subscription downloads.
- `SUBSCRIPTION_DOWNLOAD_POLL_SECONDS` and `SUBSCRIPTION_DOWNLOAD_MAX_ATTEMPTS` tune the worker.
- Item-level pause/resume UI remains a polish gap; subscription-level enable/auto-queue controls are available now.

### Follow-Up

- Phase 6: add richer subscription detail/polish, storage readouts, and item-level controls if needed.

## YouTube Subscription Phase 6 Item Controls And Readouts

Time: 15:55 KST
Commit: `pending`
Status: verified

### Summary

- Added subscription item queue, skip, and retry API routes.
- Aligned retryable subscription download failures with the item state machine by requeuing retryable failures and leaving `failed` for retry-cap/no-retry states.
- Added per-subscription storage byte/human readouts to subscription API payloads.
- Added expandable subscription item rows in the sidebar with item status, progress, detail text, and queue/skip/retry buttons.
- Updated the YouTube subscription design document from partially implemented plan to implemented MVP reference.

### Code And Docs

- Backend: `app/db.py`, `app/subscriptions.py`, `app/main.py`
- Frontend/addon: `app/templates/index.html`, `app/static/style.css`
- Tests: `tests/test_youtube_subscriptions.py`, `tests/test_review_fixes.py`
- Docs: `README.md`, `PATCH_NOTES.md`, `docs/youtube-subscriptions-design-2026-07-02.md`, `docs/index.md`, `docs/feature-code-map.md`, `docs/architecture.md`

### Verification

- `python3 -m py_compile app/main.py app/db.py app/subscriptions.py app/downloader.py app/internal_jobs.py app/parsers.py app/workflows.py app/metadata.py app/utils.py`: passed
- Inline `app/templates/index.html` script syntax check with Node after replacing Jinja JSON constants: passed
- Chrome extension JS and manifest checks: passed
- `python3 -m pytest -q -p no:cacheprovider tests/test_youtube_subscriptions.py tests/test_review_fixes.py::test_home_template_declares_subscription_sidebar_ui`: `13 passed`
- `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py tests/test_downloader_runtime.py tests/test_youtube_subscriptions.py`: `108 passed`
- `python3 -m pytest -q -p no:cacheprovider`: `143 passed`
- Dev server smoke on `127.0.0.1:8092` with temp `/data` and `/config`: home HTML contained subscription tab/item-control anchors; `/api/subscriptions` returned running check/download schedulers.
- `git diff --check`: passed

### Deploy Impact

- Adds three item action APIs and additive response fields on subscription payloads.
- Existing subscription rows do not need migration beyond the Phase 5 schema. Retryable failed items now return to `queued` with `next_attempt_at`; terminal failures stay `failed`.

### Handoff Notes

- Item actions are subscription-local and do not create normal `jobs` rows.
- Storage readouts come from `subscription_items.total_bytes/progress_bytes`, not a live filesystem scan.
- Duplicate detection against existing one-shot YouTube archives remains future optional work.

### Follow-Up

- Optional later polish: per-subscription format/profile overrides or library-index-backed duplicate detection if real usage needs them.

## YouTube Subscription Main Panel Design

Time: 16:46 KST
Commit: `c4cefa6`
Status: verified

### Summary

- Implemented switching the main work-list area to subscription-specific work when the sidebar `구독` tab is active.
- Added aggregate `GET /api/subscriptions/items` with status filtering, optional subscription scope, cursor paging, counts, scheduler status, subscription metadata, and display progress fields.
- Added the main `구독 작업 목록` table/mobile card UI with filters, gated polling, and queue/skip/retry controls.
- Kept normal `jobs` and subscription `subscription_items` operationally separate, and kept YouTube storage layout unchanged.

### Code And Docs

- Backend: `app/db.py`, `app/subscriptions.py`, `app/main.py`
- Frontend/addon: `app/templates/index.html`, `app/static/style.css`
- Tests: `tests/test_youtube_subscriptions.py`, `tests/test_review_fixes.py`
- Docs: `README.md`, `docs/youtube-subscription-main-panel-design-2026-07-02.md`, `docs/index.md`, `docs/youtube-subscriptions-design-2026-07-02.md`, `docs/feature-code-map.md`, `docs/architecture.md`

### Verification

- `python3 -m py_compile app/main.py app/db.py app/subscriptions.py app/downloader.py app/internal_jobs.py app/parsers.py app/workflows.py app/metadata.py app/utils.py`: passed
- Inline `app/templates/index.html` script syntax check with Node after replacing Jinja JSON constants: passed
- `PYTHONPATH=. pytest tests/test_youtube_subscriptions.py tests/test_review_fixes.py::test_home_template_declares_subscription_sidebar_ui`: `16 passed`
- `PYTHONPATH=. pytest tests/test_review_fixes.py tests/test_downloader_runtime.py tests/test_youtube_subscriptions.py`: `113 passed`
- `PYTHONPATH=. pytest`: `148 passed`
- `node --check chrome-extension/shared.js && node --check chrome-extension/background.js && node --check chrome-extension/popup.js && node -e "JSON.parse(require('fs').readFileSync('chrome-extension/manifest.json','utf8'))"`: passed
- Local server smoke with temporary `DATA_ROOT`/`DB_PATH`: home HTML contains `subscription-work-section`, `activeWorkMode`, the active subscription filter, and `/api/subscriptions/items`; `GET /api/subscriptions/items?status=active` returns `ok: true`
- Playwright desktop/mobile smoke: selecting sidebar `구독` hides `#jobs-section`, shows `#subscription-work-section`, and renders `구독 작업 목록`
- `git diff --check`: passed
- `rg -n "youtube-subscription-main-panel-design|YouTube Subscription Main Panel Design|/api/subscriptions/items" docs`: found expected references

### Deploy Impact

- Adds an additive subscription item aggregate API and a main-panel UI mode switch. No archive file moves or normal `jobs` rows are created for subscription items.

### Handoff Notes

- This design intentionally does not change `/data/gallery-dl/youtube.com/...` folder placement.
- Normal `jobs` and subscription `subscription_items` renderers remain separate, even where they share small formatting helpers.

### Follow-Up

- Post-integration storage-folder search checks were run after this change; no conflict was found with the folder search/create/move UI.

## Storage Folder Search Design

Time: 16:49 KST
Commit: `1cd733d`
Status: verified

### Summary

- Documented a future UI change that uses the red-marked lower sidebar area as a `저장 폴더` search box.
- Scoped the first implementation to searching currently loaded folder tree entries, with a later optional `/api/folders/search` for deeper `/data` searches.
- Moved folder creation in the design to the folder-tree right-click menu's `새 폴더` action with a folder-name popup.
- Specified that the `이동` context-menu action should open a folder-tree destination picker with `확인` and `취소`, replacing the current text prompt.
- Captured keyboard behavior, mobile layout rules, context-menu/modal code touchpoints, tests, and path-safety expectations.

### Code And Docs

- Docs: `docs/storage-folder-search-design-2026-07-02.md`, `docs/index.md`

### Verification

- `git diff --check`: passed
- `rg -n "storage-folder-search-design|Storage Folder Search Design|저장 폴더 검색창|새 폴더|이동 대상 선택|folder-create-modal|folder-move-modal|window.prompt|folder_search" docs/storage-folder-search-design-2026-07-02.md docs/patch-notes/2026-07-02.md docs/index.md`: found expected references

### Deploy Impact

- None. Documentation only.

### Handoff Notes

- The design intentionally avoids promising full `/data` search in the first client-only pass because the current folder tree is capped by `build_folder_tree(max_depth=4, max_entries=300)`.
- When implementing, split the existing folder selection click logic into a shared `selectFolderPath(path)` helper so tree clicks and search-result clicks stay consistent.
- The folder creation flow should live in the folder context menu, not in the lower search form.
- The move destination picker should keep its selected destination state separate from the main selected download folder.

### Follow-Up

- Implement the client-side loaded-tree search first; add a backend search endpoint only if real use needs deep folder search.

## Storage Folder Search Implementation

Time: 17:13 KST
Commit: `1cd733d`
Status: implemented

### Summary

- Replaced the lower left sidebar `새 폴더` form with a `저장 폴더` search box for the currently loaded folder tree.
- Added folder-tree right-click `새 폴더`, with a modal that asks for only the new folder name and creates it under the clicked parent.
- Added `POST /api/folders` for JSON folder creation while keeping legacy `POST /folders`.
- Replaced the move destination text prompt with a folder-tree destination picker modal.
- Added focused template/API regression coverage.

### Code And Docs

- Backend: `app/main.py`
- Frontend/addon: `app/templates/index.html`, `app/static/style.css`
- Tests: `tests/test_review_fixes.py`
- Docs: `README.md`, `PATCH_NOTES.md`, `docs/storage-folder-search-design-2026-07-02.md`, `docs/index.md`, `docs/feature-code-map.md`, `docs/architecture.md`

### Verification

- `python3 -m py_compile app/main.py app/db.py app/subscriptions.py`: passed
- Inline `app/templates/index.html` script syntax check with Node after replacing Jinja JSON constants: passed
- `node --check chrome-extension/shared.js`: passed
- `node --check chrome-extension/background.js`: passed
- `node --check chrome-extension/popup.js`: passed
- `python3 -m json.tool chrome-extension/manifest.json`: passed
- `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py::test_home_template_declares_storage_folder_search_ui tests/test_review_fixes.py::test_api_create_folder_creates_child_and_rejects_nested_name`: `2 passed`
- `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py`: `41 passed`
- `python3 -m pytest -q -p no:cacheprovider tests/test_youtube_subscriptions.py`: `15 passed`
- `python3 -m pytest -q -p no:cacheprovider`: `145 passed`
- `git diff --check`: passed

### Deploy Impact

- UI-only plus additive API route. No storage migration.

### Handoff Notes

- Folder search intentionally covers currently loaded sidebar folders only; deeper server-wide search remains optional.
- New folder creation rejects nested names in the API and keeps `/data` path resolution through existing helpers.
- Move still relies on `/api/fs/move` for final server-side safety checks.

### Follow-Up

- Optional later: add `/api/folders/search` if real archives outgrow the loaded tree cap.

## Storage Folder Search UI Recheck

Time: 18:20 KST
Commit: `pending`
Status: verified

### Summary

- Re-reviewed the storage folder search/create/move UI after `c4cefa6` added the subscription main work list to the same template and stylesheet.
- Confirmed the lower sidebar folder search, tree-context `새 폴더`, `POST /api/folders`, and move destination tree modal remain wired correctly.
- Updated `docs/storage-folder-search-design-2026-07-02.md` so it reads as an implemented reference instead of an active proposal.

### Code And Docs

- Reviewed code: `app/templates/index.html`, `app/static/style.css`, `app/main.py`, `tests/test_review_fixes.py`
- Docs: `docs/storage-folder-search-design-2026-07-02.md`, `docs/patch-notes/2026-07-02.md`

### Verification

- `python3 -m py_compile app/main.py app/db.py app/subscriptions.py`: passed
- Inline `app/templates/index.html` script syntax check with Node after replacing Jinja JSON constants: passed
- Chrome extension JS and manifest checks: passed
- `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py::test_home_template_declares_storage_folder_search_ui tests/test_review_fixes.py::test_api_create_folder_creates_child_and_rejects_nested_name tests/test_review_fixes.py::test_home_template_declares_subscription_sidebar_ui`: `3 passed`
- `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py`: `41 passed`
- `python3 -m pytest -q -p no:cacheprovider`: `148 passed`
- `git diff --check`: passed
- `rg -n "window\.prompt\('이동할 대상 폴더|이동할 대상 폴더를 /data 기준 경로로 입력하세요\." app/templates/index.html app/static/style.css tests/test_review_fixes.py`: found only the negative regression assertion in `tests/test_review_fixes.py`

### Deploy Impact

- Documentation-only recheck. No runtime behavior change.

### Handoff Notes

- `AGENTS.md` and `README_LLM.md` were reviewed; no recurring workflow rule changed, so they do not need edits.
- `README.md`, `docs/feature-code-map.md`, and `docs/architecture.md` already describe the implemented storage folder search/API flow.

### Follow-Up

- Optional later: run a browser visual smoke on real NAS folder data if deep folder counts make the loaded-tree search feel too narrow.

## Civitai Image Card Media Viewer UX

Time: 16:06 KST
Commit: `pending`
Status: verified

### Summary

- Treated Civitai image page cards as media archives immediately after download, before a library index scan has populated `has_media`.
- Made every media-capable library card open the media viewer on primary click, while preserving explicit card action buttons such as URL open and favorite.
- Kept keyboard Enter/Space behavior and media viewer focus restore aligned with the same `data-media-archive` marker.

### Code And Docs

- Frontend/addon: `app/templates/index.html`, `app/static/style.css`
- Tests: `tests/test_civitai_viewer_health.py`
- Docs: `docs/patch-notes/2026-07-02.md`

### Verification

- `python3 -m pytest -q -p no:cacheprovider tests/test_civitai_viewer_health.py`: `6 passed`
- `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py`: `39 passed`
- `python3 -m pytest -q -p no:cacheprovider`: `143 passed`
- Inline `app/templates/index.html` script syntax check with Node after replacing Jinja JSON constants: passed
- Python compile for core modules plus Chrome extension JS and manifest checks: passed
- `git diff --check`: passed

### Deploy Impact

- None beyond the web UI behavior change.

### Handoff Notes

- Civitai image page job rows use `model_category="Civitai Image Page"`; the frontend now recognizes that as media-capable even when `has_media` is not present yet.
- Media-capable card behavior should continue to key off `data-media-archive="true"` rather than the CSS class alone.

### Follow-Up

- None.
