# 2026-07-09 Patch Notes

## ComfyUI Transfer Civitai Folder Context Fix

Time: late KST
Commit: pending
Status: verified

### Summary

- Changed Civitai model archive transfer destination handling so LoRA/checkpoint archive folders no longer arrive in ComfyUI as bare `version_<id>` folders.
- For Civitai model archive folders with `_civitai_metadata.json`, normal transfer now appends the source model folder before the archive version folder.
- For Civitai image `사용 리소스 전송`, primary model-file transfers now append `model/version_<id>` under the mapped ComfyUI destination before the filename.
- Confirmed this matches ComfyUI/LoRA Manager's relative-path model lookup direction: ComfyUI detects LoRAs under `ComfyUI/models/loras`, and LoRA Manager supports full-path LoRA names/folders instead of flattening duplicate filenames.

### Code And Docs

- Backend: updated `app/main.py` transfer destination subpath handling for Civitai model archive folders and primary resource files.
- Tests: updated `tests/test_transfer_api.py` for normal archive-folder transfer and Resources-used primary-file transfer.
- Docs: updated `README_LLM.md`, `docs/configuration.md`, `docs/architecture.md`, `docs/operations.md`, `docs/feature-code-map.md`, and this patch note.

### Verification

- Command/result: `python3 -m py_compile app/main.py app/transfer.py` passed.
- Command/result: `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q -p no:cacheprovider tests/test_transfer_api.py tests/test_transfer_core.py` passed, 49 tests.
- Command/result: `python3 -m py_compile app/main.py app/db.py app/defaults.py app/downloader.py app/internal_jobs.py app/parsers.py app/workflows.py app/metadata.py app/utils.py app/subscriptions.py` passed.
- Command/result: `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py` passed, 101 tests with one existing Starlette deprecation warning.
- Command/result: `git diff --check` passed.

### Deploy Impact

- No operator action required. Existing transfer targets keep their current policies; new transfer jobs for Civitai model archives use the more descriptive destination path.

### Handoff Notes

- The fix is intentionally limited to Civitai model archives identified by `_civitai_metadata.json` under mapped `stable-diffusion/<route>` paths. Generic folders keep the previous copy shape.

### Follow-Up

- None.

## Media Maintenance Subagent Audit Follow-Up

Time: 13:27 KST
Commit: `bcb939f`
Status: verified

### Summary

- Ran three Codex subagent audits over backend/DB/internal jobs, frontend/API contract, and docs/operations consistency.
- Changed media cache quota cleanup and manual cache clear so active temp cache artifacts are not removed; stale temp cleanup remains TTL-driven.
- Changed scoped library reindex failure handling so `library.indexing` is reset and selected-folder `library_folder_state` is marked `failed` with the error detail.
- Preserved `LIBRARY_WATCHER_LOCAL_ONLY` as env/settings fallback by removing the settings-save path that forced it to `1` despite no UI field.
- Fixed docs found stale or incomplete by the subagent audits: maintenance setting references, internal job concurrency wording, additive migration wording, verification baselines including `app/defaults.py`, and media-library comparison status.

### Code And Docs

- Backend: updated `app/main.py` cache cleanup and reindex failure handling.
- Tests: added regression coverage in `tests/test_review_fixes.py`.
- Docs: updated `README_LLM.md`, `SKILL_Dev/skill_build.md`, `docs/development.md`, `docs/configuration.md`, `docs/feature-code-map.md`, `docs/media-library-scaling-comparison-2026-07-09.md`, `docs/operations.md`, and this patch note.

### Verification

- Command/result: `python3 -m py_compile app/main.py app/db.py app/defaults.py app/internal_jobs.py` passed.
- Command/result: `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py -k 'media_cache or settings_post_saves or policy_status or reindex_failure or reindex_job_resumes or maintenance_policy or startup_cleanup'` passed, 9 tests with 92 deselected.
- Command/result: `git diff --check` passed.
- Command/result: `python3 -m py_compile app/main.py app/db.py app/defaults.py app/downloader.py app/internal_jobs.py app/parsers.py app/workflows.py app/metadata.py app/utils.py app/subscriptions.py` passed.
- Command/result: `node --check chrome-extension/shared.js`, `background.js`, and `popup.js` passed; Chrome extension manifest JSON parse passed.
- Command/result: rendered `app/templates/index.html` through `TestClient`, extracted the inline script to `/tmp/hugcivi-rendered-index-script.js`, and `node --check /tmp/hugcivi-rendered-index-script.js` passed.
- Command/result: `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py tests/test_downloader_runtime.py` passed, 173 tests with one existing Starlette deprecation warning.
- Command/result: `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q -p no:cacheprovider` passed, 293 tests with one existing Starlette deprecation warning.

### Deploy Impact

- No additional operator action. This is a safety/doc correction on top of the additive media maintenance baseline.

### Handoff Notes

- Quota/manual cache cleanup intentionally skips temp cache files. If temp files need removal, use TTL cleanup after they are stale.

### Follow-Up

- None for the subagent audit findings.

## Media Cache And Maintenance Policy Follow-Up

Time: 13:12 KST
Commit: `1fd7b84`
Status: verified

### Summary

- Added media cache status and manual cleanup APIs plus settings-pane controls. Cache quota cleanup now removes least-recently-accessed files first, and cached media responses update access time when safe.
- Added UI/runtime settings for media cache TTL/quota, thumbnail backfill defaults, and heavy internal job maintenance policy.
- Added `INTERNAL_JOB_MAINTENANCE_MODE=immediate|window|paused` support in the internal job scheduler. Heavy ZIP/library/media/thumbnail jobs can be deferred outside a server-local maintenance window or kept queued while paused.
- Added `library_folder_state` as an additive SQLite table for selected-folder reindex progress. Selected-folder index responses can include additive `index_status.folder_state`.
- Added disabled-by-default policy/status endpoints for future local filesystem watcher and video preview/trickplay work: `/api/library/watcher` and `/api/media/video-preview`. This build stores/reports policy only; it does not start a watcher or preview worker.
- Strengthened docs guidance that `/config` should be local/fast storage while `/data` can be large NAS/archive storage.

### Code And Docs

- Backend/DB: updated `app/db.py`, `app/defaults.py`, `app/internal_jobs.py`, and `app/main.py` for cache cleanup, maintenance scheduling, folder scan state, and policy status APIs.
- Frontend: updated `app/templates/index.html` with the settings `유지보수` pane and cache cleanup buttons.
- Tests: updated `tests/test_review_fixes.py` for cache LRU/API behavior, settings save behavior, maintenance gating, folder scan state, policy status endpoints, and template markers.
- Docs: updated `README_LLM.md`, `docs/media-library-scaling-comparison-2026-07-09.md`, `docs/configuration.md`, `docs/architecture.md`, `docs/feature-code-map.md`, and `docs/operations.md`.

### Verification

- Command/result: `python3 -m py_compile app/main.py app/db.py app/internal_jobs.py app/defaults.py` passed.
- Command/result: `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py -k 'media_cache or maintenance_policy or policy_status or reindex_job_resumes or settings_post_saves or maintenance_cache_controls or startup_cleanup or deferred_thumbnail_queue'` passed, 9 tests with 90 deselected.
- Command/result: `git diff --check` passed.
- Command/result: `python3 -m py_compile app/main.py app/db.py app/downloader.py app/internal_jobs.py app/parsers.py app/workflows.py app/metadata.py app/utils.py app/subscriptions.py app/defaults.py` passed.
- Command/result: `node --check chrome-extension/shared.js`, `background.js`, and `popup.js` passed; Chrome extension manifest JSON parse passed.
- Command/result: rendered `app/templates/index.html` through `TestClient`, extracted the inline script to `/tmp/hugcivi-rendered-index-script.js`, and `node --check /tmp/hugcivi-rendered-index-script.js` passed.
- Command/result: `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py` passed, 99 tests with one existing Starlette deprecation warning.
- Command/result: `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q -p no:cacheprovider tests/test_downloader_runtime.py` passed, 72 tests.
- Command/result: `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q -p no:cacheprovider` passed, 291 tests with one existing Starlette deprecation warning.

### Deploy Impact

- Additive SQLite migration: `library_folder_state`.
- No required operator action. Operators may optionally configure cache quota/TTL or heavy-job maintenance windows from the settings modal or environment.
- Existing `/config/media-cache` files remain valid; quota cleanup may remove old cache files when `MEDIA_CACHE_MAX_BYTES` is set.

### Handoff Notes

- Watcher and video preview/trickplay controls are intentionally policy/status-only in this build. Do not assume they start background workers until a later implementation adds those workers and tests.

### Follow-Up

- Actual local-filesystem watcher and video preview/trickplay workers remain future work behind the policy/status controls added here.

## Media Library Scaling Research And Follow-Up Plan

Time: 13:00 KST
Commit: `98866cf`
Status: verified

### Summary

- Compared HugCivi's large library direction against public Jellyfin/Plex documentation for media libraries, server data directories, background scans, cache cleanup, and preview thumbnail generation.
- Recorded the result in [Media Library Scaling Comparison 2026-07-09](../media-library-scaling-comparison-2026-07-09.md).
- Selected six follow-up areas before implementation: stronger local `/config` guidance, cache quotas/cleanup, maintenance windows, folder-level scan state, optional filesystem watcher, and opt-in video preview/trickplay handling.

### Code And Docs

- Docs: added the comparison/planning reference and linked it from `docs/index.md`.

### Verification

- Command/result: `git diff --check` passed.
- Command/result: `python3 -m py_compile app/main.py app/db.py app/downloader.py app/internal_jobs.py app/parsers.py app/workflows.py app/metadata.py app/utils.py app/subscriptions.py` passed.
- Command/result: `node --check chrome-extension/shared.js`, `background.js`, and `popup.js` passed; Chrome extension manifest JSON parse passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider` passed, 285 tests with one existing Starlette deprecation warning.

### Deploy Impact

- No runtime behavior change.

### Handoff Notes

- Keep HugCivi's archive/source/sidecar model distinct from Jellyfin/Plex movie/show/music identity models. The follow-up work should adapt their cache and maintenance patterns without replacing HugCivi's source-aware archive browser shape.

### Follow-Up

- Implement the six recorded cache/index maintenance follow-ups after committing the current DB/reindex/frontend optimization set.

## Library Reindex Speed And Reliability Follow-Up

Time: 11:45 KST
Commit: `98866cf`
Status: verified

### Summary

- Kept the DB-first selected-folder direction: normal `index` navigation still does not auto-live-scan NAS folders.
- Changed `POST /api/library/reindex` to coalesce duplicate active `library_reindex` jobs for the same normalized `path`/`source_group`/`category` scope. Duplicate active requests return the existing `job_id` and additive `deduped: true`.
- Changed library scan batches to collect index records and write them through one bulk `library_items` upsert transaction per batch instead of committing each indexed path separately.
- Changed indexed library sort SQL to use stored `sort_title`, `path`, and `mtime_ns` columns instead of expression sorting with `lower(...)`/`CASE`, and added date/source/category helper indexes.
- Changed the browser library refresh flow to track the returned reindex `job_id` and poll `/api/jobs/{job_id}` directly, so completion refresh no longer depends only on the currently visible jobs page/filter.

### Code And Docs

- Backend/DB: updated `app/main.py` reindex enqueueing and scan batching, plus `app/db.py` internal-job reuse, bulk library upserts, sort SQL, path range filters, and additive indexes.
- Frontend: updated `app/templates/index.html` to track and poll reindex job IDs independently of the visible jobs page.
- Tests: updated `tests/test_review_fixes.py` for active-scope dedupe, terminal/new-scope behavior, bulk upsert batching, index-backed sort SQL, and frontend polling markers.
- Docs: updated this patch note and refreshed `README_LLM.md`, `docs/feature-code-map.md`, `docs/configuration.md`, `docs/architecture.md`, and `docs/operations.md`.

### Verification

- Command/result: `python3 -m py_compile app/main.py app/db.py app/internal_jobs.py` passed.
- Command/result: `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py -k 'library_reindex or library_scan_batch or library_index_global_sort or home_template'` passed, 10 tests with 83 deselected.
- Command/result: `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py -k library` passed, 34 tests with 59 deselected.
- Command/result: `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py tests/test_downloader_runtime.py` passed, 165 tests with one existing Starlette deprecation warning.
- Command/result: `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q -p no:cacheprovider` passed, 285 tests with one existing Starlette deprecation warning.
- Command/result: rendered `app/templates/index.html` through `TestClient` with temp `DB_PATH`/`DATA_ROOT`/`CONFIG_ROOT`, extracted the script, and `node --check /tmp/hugcivi-rendered-index-script.js` passed.
- Command/result: `node --check chrome-extension/shared.js`, `background.js`, and `popup.js` passed; Chrome extension manifest JSON parse passed.
- Command/result: `git diff --check` passed.

### Deploy Impact

- No operator action required. Duplicate refresh clicks/API calls for the same active library scope now reuse the active reindex job, reducing redundant NAS scans and DB writes.

### Handoff Notes

- `/api/library/reindex` now returns additive `deduped`; keep callers tolerant of either `false` for new jobs or `true` for reused active jobs.

## Library DB-Only Entry And Async Reindex

Time: later KST
Commit: `fce212f`
Status: verified

### Summary

- Changed normal library/category `index` mode so selected-folder pages query SQLite only and return fast `index_status.needs_refresh`/`refreshing` when indexed rows are missing instead of automatically live-scanning NAS folders.
- Kept explicit `/api/library?mode=live&path=...` as the manual recovery path for filesystem-backed cards.
- Changed `POST /api/library/reindex` to queue a `library_reindex` internal job with scoped `path`, `source_group`, and `category` payload instead of scanning synchronously in the HTTP request.
- Added scoped reindex cursor reuse across batches, scan-state queued/running markers, and a scan lock around background indexer/reindex job scan batches.
- Added source/category path indexes for library scope queries.
- Updated the web UI to cancel stale library fetches with `AbortController`, remove forced selected-folder `mode=live` refreshes, show index refresh status, and refresh the active DB page when a matching `library_reindex` job completes.

### Code And Docs

- Backend: updated `app/main.py` library index/reindex behavior and added `library_reindex` internal job handling.
- DB: added additive `library_items` indexes in `app/db.py`.
- Frontend: updated `app/templates/index.html` library refresh, polling, empty-state, and fetch cancellation behavior.
- Tests: updated library/index tests for DB-only index mode and async reindex jobs.
- Docs: updated `README_LLM.md`, `docs/index.md`, `docs/library-category-index-refresh-plan-2026-07-08.md`, `docs/feature-code-map.md`, `docs/configuration.md`, `docs/architecture.md`, and `docs/operations.md`.

### Verification

- Command/result: `python3 -m py_compile app/main.py app/db.py app/internal_jobs.py` passed.
- Command/result: `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py -k 'library or internal_job'` passed, 36 tests with 54 deselected.
- Command/result: `python3 -m py_compile app/main.py app/db.py app/internal_jobs.py app/downloader.py app/parsers.py app/workflows.py app/metadata.py app/utils.py app/subscriptions.py` passed.
- Command/result: `node --check chrome-extension/shared.js`, `background.js`, and `popup.js` passed; Chrome extension manifest JSON parse passed.
- Command/result: `git diff --check` passed.
- Command/result: `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q -p no:cacheprovider tests/test_downloader_runtime.py` passed, 72 tests.
- Command/result: `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py` passed, 90 tests with one existing Starlette deprecation warning.
- Command/result: `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q -p no:cacheprovider` passed, 282 tests with one existing Starlette deprecation warning.

### Deploy Impact

- First entry into an unindexed category/folder now returns immediately with an empty/refresh-needed DB page instead of blocking on a live filesystem scan.
- Operators should use the library `갱신` button or `POST /api/library/reindex` and watch the queued `library_reindex` job when restored or externally modified files need to appear in normal index mode.

### Handoff Notes

- Explicit live mode remains available for manual recovery and debugging. Do not reintroduce automatic live fallback into normal `index` navigation without an explicit product decision, because that is the NAS entry-latency bottleneck this change removes.

## Civitai Workflow URL Snapshot

Time: 00:32 KST
Commit: `d006fae`
Status: verified

### Summary

- Added a dated snapshot document for three Civitai `Workflows` archive URLs:
  - `https://civitai.red/models/2031587/qwenedit2509-simple-consistent-character-pose-changer-workflow`
  - `https://civitai.red/models/1890385/qwen-image-edit-multi-gen`
  - `https://civitai.red/models/1944963/qwen-clothing-tranfer-comfyui-workflow`
- Recorded each model ID, selected version, primary ZIP file, expected HugCivi `ComfyUI Workflow` classification, expected `/data/civitai/workflows/...` path, and the observed unauthenticated `401 Unauthorized` download status.
- Linked the snapshot from `docs/index.md`.

### Code And Docs

- Docs: added `docs/civitai-workflow-archive-url-check-2026-07-09.md` and updated `docs/index.md`.

### Verification

- Command/result: Civitai API metadata check passed for all three model IDs; each currently reports `type=Workflows` with primary `Archive`/`Other` ZIP and classifies as `ComfyUI Workflow` through current HugCivi code.
- Command/result: `git diff --check` passed.
- Command/result: `python3 -m py_compile app/main.py app/db.py app/downloader.py app/internal_jobs.py app/parsers.py app/workflows.py app/metadata.py app/utils.py app/subscriptions.py` passed.
- Command/result: `node --check chrome-extension/shared.js`, `background.js`, and `popup.js` passed; Chrome extension manifest JSON parse passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider` passed, 280 tests with one existing Starlette deprecation warning.

### Deploy Impact

- No runtime behavior change. The next pushed image includes the new snapshot docs.

### Handoff Notes

- The snapshot confirms routing/classification only. End-to-end file extraction still requires a valid `CIVITAI_TOKEN` because unauthenticated download endpoints returned 401 for all three current version IDs.

### Follow-Up

- Verify actual ZIP extraction in an environment with a configured Civitai token if needed.
