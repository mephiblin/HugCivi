# 2026-07-09 Patch Notes

## Civitai Workflow URL Snapshot

Time: 00:32 KST
Commit: `pending`
Status: verified

### Summary

- Added a dated snapshot document for three Civitai `Workflows` archive URLs:
  - `https://civitai.red/models/2031587/qwenedit2509-simple-consistent-character-pose-changer-workflow`
  - `https://civitai.red/models/1890385/qwen-image-edit-multi-gen`
  - `https://civitai.red/models/1944963/qwen-clothing-tranfer-comfyui-workflow`
- Recorded each model ID, selected version, primary ZIP file, expected HugCivi `ComfyUI Workflow` classification, expected `/data/civitai/workflows/...` path, and the observed unauthenticated `401 Unauthorized` download status.
- Linked the snapshot from `docs/index.md`.

### Code And Docs

- Docs: added `docs/civitai-workflow-archive-url-check-2026-07-09.md` and updated `docs/index.md`.

### Verification

- Command/result: Civitai API metadata check passed for all three model IDs; each currently reports `type=Workflows` with primary `Archive`/`Other` ZIP and classifies as `ComfyUI Workflow` through current HugCivi code.
- Command/result: `git diff --check` passed.
- Command/result: `python3 -m py_compile app/main.py app/db.py app/downloader.py app/internal_jobs.py app/parsers.py app/workflows.py app/metadata.py app/utils.py app/subscriptions.py` passed.
- Command/result: `node --check chrome-extension/shared.js`, `background.js`, and `popup.js` passed; Chrome extension manifest JSON parse passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider` passed, 280 tests with one existing Starlette deprecation warning.

### Deploy Impact

- No runtime behavior change. The next pushed image includes the new snapshot docs.

### Handoff Notes

- The snapshot confirms routing/classification only. End-to-end file extraction still requires a valid `CIVITAI_TOKEN` because unauthenticated download endpoints returned 401 for all three current version IDs.

### Follow-Up

- Verify actual ZIP extraction in an environment with a configured Civitai token if needed.
