## Civitai Rendered Webm Image Pages

Time: 00:19 KST
Commit: `1077c0e`
Status: verified

### Summary

- Civitai image-page downloads now handle rendered-page video entries such as `https://civitai.red/images/97376108`, where the public API returns no items and the rendered page exposes a `VideoObject` plus a webm asset ID.
- The downloader reconstructs the original Civitai media URL for rendered webm videos, saves the archive as `.webm`, and keeps the JSON-LD poster URL as the archive thumbnail.

### Code And Docs

- Backend: widened Civitai image-page metadata extraction in `app/downloader.py` to accept `VideoObject`, derive original media URLs from rendered page data, preserve video content types/extensions, and store video-aware sidecar fields.
- Frontend/addon: None.
- Tests: added a downloader runtime regression for the `civitai.red/images/97376108` rendered webm shape.
- Docs: updated `docs/feature-code-map.md` Civitai image archive ownership/state notes.

### Verification

- Command/result: `python3 -m py_compile app/downloader.py tests/test_downloader_runtime.py` passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_downloader_runtime.py -k 'civitai_image_page'` passed, 9 passed / 61 deselected.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_bulk_add.py tests/test_civitai_image_parser.py tests/test_civitai_viewer_health.py tests/test_hitomi_listing.py tests/test_asmrone_provider.py tests/test_youtube_parser.py tests/test_main_urls.py tests/test_downloader_runtime.py` passed, 118 passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider` passed, 195 passed / 1 warning.

### Deploy Impact

- None until the next image build/deploy.

### Handoff Notes

- The user supplied a Civitai token during triage; it was not written to code, docs, commands, or logs. The bug reproduced without a token because the rendered page and CDN media URL were publicly reachable.
- The feature is still named "Civitai image page" in code/UI, but the archived primary asset may now be an image or video file.

### Follow-Up

- None.

## Library Date Sort Directions

Time: 00:33 KST
Commit: `c97c675`
Status: verified

### Summary

- Split the library card date sort into explicit newest-first and oldest-first options.
- Kept legacy `sort=date` API requests working as a newest-first alias.

### Code And Docs

- Backend: normalized `date_desc`/`date_asc` library sort values in `app/main.py` and applied matching SQLite index ordering in `app/db.py`.
- Frontend/addon: replaced the ambiguous date dropdown option in `app/templates/index.html` with separate newest and oldest options; kept client fallback sorting compatible with legacy `date`.
- Tests: added a library API regression covering indexed newest/oldest sorting, selected-folder live oldest sorting, and the legacy `date` alias.
- Docs: updated `docs/architecture.md` and `docs/feature-code-map.md`.

### Verification

- Command/result: `python3 -m py_compile app/main.py app/db.py` passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py -k 'library_api_date_sort'` passed, 1 passed / 63 deselected.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py` passed, 64 passed / 1 warning.
- Command/result: `node --check chrome-extension/shared.js`, `node --check chrome-extension/background.js`, `node --check chrome-extension/popup.js`, and `node -e "JSON.parse(require('fs').readFileSync('chrome-extension/manifest.json','utf8'))"` passed.
- Command/result: `git diff --check` passed.

### Deploy Impact

- None until the next image build/deploy.

### Handoff Notes

- The API now echoes `date_desc` for legacy `sort=date`; ordering behavior stays the same as the old date sort.

### Follow-Up

- None.

## Selected Folder Live Pagination Stability

Time: 00:35 KST; review follow-up 01:10 KST
Commit: `c97c675`
Status: verified

### Summary

- Fixed selected-folder live library pages repeating cards across page boundaries when sorting, which could make Japanese/Chinese-titled cards appear to remain on the next page.
- The previous overlay CSS fix only affected text fitting; the page-repeat root cause was live pagination sorting an expanding partial scan set.
- Page and sort clicks now clear the current card grid into the existing loading state while the new library page is fetched, so old CJK-titled cards do not visually linger during the transition.
- Long library card titles now clamp to two lines with a max-height fallback, keeping Japanese/Chinese titles inside the card overlay.

### Code And Docs

- Backend: changed `live_library_items_page()` in `app/main.py` to sort a stable live-scan window before slicing page results.
- Frontend/addon: updated `app/templates/index.html` to use the loading-state path for library sort/page navigation, and `app/static/style.css` to clamp long card titles.
- Tests: added a regression for disjoint first/second live pages with CJK-titled cards, plus template/CSS assertions for the loading-state navigation path and overlay clamp, in `tests/test_review_fixes.py`.
- Docs: updated `docs/architecture.md` and `docs/feature-code-map.md`.

### Verification

- Command/result: `python3 -m py_compile app/main.py app/db.py tests/test_review_fixes.py` passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py -k 'library_api_date_sort or live_library_pagination or api_folders_default or folder_tree_large_sibling or home_template_declares_storage_folder_search_ui'` passed, 5 passed / 59 deselected.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py` passed, 64 passed / 1 warning.
- Command/result: `node --check chrome-extension/shared.js && node --check chrome-extension/background.js && node --check chrome-extension/popup.js && node -e "JSON.parse(require('fs').readFileSync('chrome-extension/manifest.json','utf8'))"` passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider` passed, 198 passed / 1 warning.
- Command/result: `git diff --check` passed.

### Deploy Impact

- None until the next image build/deploy.

### Handoff Notes

- Live selected-folder pagination is still bounded; very large folders beyond the stable scan window may eventually need indexed pagination, lazy loading, or keyset pagination.

### Follow-Up

- None for ordinary selected-folder pages.

## Folder Tree Large Sibling Budget

Time: 00:33 KST
Commit: `c97c675`
Status: verified

### Summary

- Fixed `/api/folders` and the initial sidebar tree dropping ordinary folders beyond the old non-root sibling budget of 120 entries.
- Raised the default folder-tree budget to 5,000 total entries and 1,000 children per non-root folder while preserving explicit low-budget parameters for regression tests.

### Code And Docs

- Backend: updated `build_folder_tree()` defaults in `app/main.py`.
- Frontend/addon: no code changes; the existing sidebar tree already scrolls and consumes the backend tree payload.
- Tests: added `/api/folders` regression coverage for 151 sibling folders in `tests/test_review_fixes.py`.
- Docs: updated `docs/feature-code-map.md` and `docs/storage-folder-search-design-2026-07-02.md`.

### Verification

- Command/result: `python3 -m py_compile app/main.py tests/test_review_fixes.py` passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py -k 'folder_tree_large_sibling or api_folders_default or api_create_folder or home_template_declares_storage_folder_search_ui'` passed, 4 passed / 59 deselected.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py` passed, 64 passed / 1 existing Starlette template deprecation warning.
- Command/result: `git diff --check` passed.

### Deploy Impact

- None until the next image build/deploy.

### Handoff Notes

- Root cause was backend truncation in `build_folder_tree()`, not sidebar CSS overflow. `.folder-tree` already scrolls on desktop/mobile, but it could only render nodes returned by `/api/folders`.
- Folder search and move-destination picking still operate on the currently loaded tree. Very deep folders beyond depth 4, trees above 5,000 entries, or a single non-root folder above 1,000 child folders still need a future lazy-load or server search design.

### Follow-Up

- Consider `/api/folders/search` or lazy child loading if real archives exceed the new tree budgets.

## Archive-Scale Folder Tree Design

Time: 00:53 KST
Commit: `pending`
Status: planning

### Summary

- Documented the long-term folder navigation direction for archive-scale collections.
- Clarified that increasing `max_children_per_folder` is only a compatibility guardrail, not the primary scaling strategy.
- Proposed lazy child loading, server-side folder search, and an optional SQLite folder index for large archives.

### Code And Docs

- Backend: No code changes.
- Frontend/addon: No code changes.
- Tests: Documentation-only verification.
- Docs: added `docs/folder-tree-scaling-design-2026-07-06.md`, linked it from `docs/index.md`, and cross-referenced it from `docs/storage-folder-search-design-2026-07-02.md`.

### Verification

- Command/result: `git diff --check` passed.
- Command/result: manually inspected Markdown links for the new design document from `docs/index.md` and `docs/storage-folder-search-design-2026-07-02.md`.

### Deploy Impact

- None. Documentation-only planning record.

### Handoff Notes

- Future implementation should not make `/api/folders` return the full archive tree. Keep `/api/folders` as a bounded compatibility root tree and add lazy child/search APIs instead.

### Follow-Up

- Implement `GET /api/folders/children` and `GET /api/folders/search` when real archive sizes exceed the current eager tree budgets.

## Tree Folder Library Pagination And CJK Card Text

Time: 00:24 KST
Commit: `1077c0e`
Status: verified

### Summary

- Fixed selected-folder live library pagination showing duplicate page buttons such as `1, 1, 2` when the total page count is not known yet.
- Tightened library card overlay text wrapping so Japanese, Chinese, and mixed CJK titles stay inside the card.

### Code And Docs

- Backend: None.
- Frontend/addon: updated `renderLibraryPagination()` in `app/templates/index.html` and library card overlay text CSS in `app/static/style.css`.
- Tests: added static regression checks in `tests/test_review_fixes.py`.
- Docs: this patch note.

### Verification

- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py -k 'home_template_declares_storage_folder_search_ui or deferred_thumbnail_queue'` passed, 2 passed / 59 deselected.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py` passed, 61 passed / 1 warning.
- Command/result: `node --check chrome-extension/shared.js && node --check chrome-extension/background.js && node --check chrome-extension/popup.js && node -e "JSON.parse(require('fs').readFileSync('chrome-extension/manifest.json','utf8'))"` passed.
- Command/result: `python3 -m py_compile app/main.py app/db.py app/downloader.py app/internal_jobs.py app/parsers.py app/workflows.py app/metadata.py app/utils.py app/subscriptions.py` passed.
- Command/result: `git diff --check` passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider` passed, 195 passed / 1 warning.
- Command/result: headless Playwright DOM check passed for unknown-total pagination (`1`, `2`) and a long Japanese/Chinese card title staying within card width.

### Deploy Impact

- None until the next image build/deploy.

### Handoff Notes

- The duplicate page issue lived entirely in the frontend fallback path for live library pages where `total_pages` is `null`; indexed pages using known `total_pages` still use `visibleJobPages()`.

### Follow-Up

- None.
