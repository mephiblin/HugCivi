## Automatic Pawchive gallery-dl Routing

Time: 00:05 KST
Commit: `pending`
Status: verified

### Summary

- Pawchive `pawchive.pw` and `pawchive.st` URLs can now be pasted directly into HugCivi without a `gallery-dl` or `gdl` prefix.
- Exact root and `www` host matching routes supported Pawchive pages through the existing gallery-dl provider; unrelated subdomains and lookalike domains keep the generic HTTP fallback.

### Code And Docs

- Backend: Added exact Pawchive host recognition in `app/parsers.py`; download execution, queueing, authentication, and archive layout continue to use the existing gallery-dl path.
- Frontend/addon: No code changes; the existing URL submission surfaces inherit backend auto-routing.
- Tests: Added `tests/test_gallerydl_parser.py` coverage for `.pw`, `.st`, `www`, target subdirectories, and unrecognized subdomain fallback.
- Docs: Updated `README.md`, `README_LLM.md`, and `docs/feature-code-map.md`. Kept `docs/gallery-dl-auth.md` unchanged because it is explicitly a 2026-06-30 snapshot.

### Verification

- `python3 -m py_compile app/parsers.py`: passed.
- `python3 -m pytest -q -p no:cacheprovider tests/test_gallerydl_parser.py tests/test_bulk_add.py tests/test_civitai_image_parser.py tests/test_hitomi_listing.py tests/test_asmrone_provider.py tests/test_youtube_parser.py tests/test_main_urls.py tests/test_downloader_runtime.py`: 112 passed in 1.43s using a temporary virtual environment populated from `requirements.txt`.
- Full `python3 -m pytest -q -p no:cacheprovider`: 298 passed and 2 unrelated media tests failed (`test_media_thumbnail_backfill_job_uses_card_representatives`, `test_text_and_markdown_files_are_readable_media_cards`). Both failures reproduce unchanged in a detached clean `origin/main` worktree, while all Pawchive/provider-focused tests pass.
- Downloaded the minimum allowed `gallery-dl==1.32.4` wheel and inspected its file list: `gallery_dl/extractor/pawchive.py` is present.
- `git diff --check`: passed before the final handoff note update and rerun during final verification.

### Deploy Impact

- No database, configuration, storage-layout, or frontend changes. A rebuilt image or restarted updated application process is required for the parser change to take effect.
- The existing `gallery-dl>=1.32.4,<2.0` requirement already includes the Pawchive extractor.

### Handoff Notes

- Keep Pawchive auto-routing ahead of generic HTTP routing and limited to gallery-dl's recognized root/`www` hosts.

### Follow-Up

- The two pre-existing media test failures require separate remediation or explicit acceptance before the production build workflow proceeds.

## Explicit Commit And AMD64 Release Workflows

Time: 00:28 KST
Commit: `pending`
Status: verified

### Summary

- Split the repository build skill into `commit-push` for Git-only publication and `commit-build-push` for Git plus production image publication.
- Made `linux/amd64` mandatory for the production `latest` tag and documented ARM64-host QEMU/binfmt registration as part of `commit-build-push`.

### Code And Docs

- Backend: None.
- Frontend/addon: None.
- Skills: Reworked `SKILL_Dev/skill_build.md` with explicit workflow selection, selective commit scope, clean detached worktree builds, AMD64 manifest verification, failure handling, and final reporting.
- Docs: Updated the repo-local skill index and `README_LLM.md` routing summary.

### Verification

- `quick_validate.py` against a standard-layout copy of `skill_build.md`: passed.
- `quick_validate.py SKILL_Dev`: passed.
- `git diff --check`: passed.
- Registered AMD64 QEMU/binfmt on the ARM64 host and confirmed buildx advertises `linux/amd64`.
- Built commit `4871c37` successfully as `linux/amd64`; the local image ID was `sha256:65936398c49de928fc5a8d38d9f7301cd0312fcd0ca4a3afd44434c6d4969a03`.
- GHCR rejected the image push with `unauthorized: unauthenticated`; neither `sha-4871c37` nor `latest` was published by that attempt.

### Deploy Impact

- Development/release workflow only; no application runtime, database, configuration, or storage behavior changes.
- `commit-build-push` now treats the request as authorization to register AMD64 QEMU/binfmt on an ARM64 Docker host when required and refuses to substitute an ARM64-only production `latest` image.

### Handoff Notes

- Use `commit-push` when the user wants only Git publication.
- Use `commit-build-push` when the user wants the GHCR/Portainer image; publish both `sha-<commit>` and `latest` as `linux/amd64`.

### Follow-Up

- Run `docker login ghcr.io`, then rebuild and push the final workflow commit as both `sha-<commit>` and `latest` for `linux/amd64`.
