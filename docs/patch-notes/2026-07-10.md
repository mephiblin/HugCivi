# 2026-07-10 Patch Notes

## Completed Download Library Index Hook

Time: evening KST
Commit: pending
Status: verified

### Summary

- Added completion hooks so HugCivi-owned external downloads and subscription downloads immediately refresh the completed `target_dir` in the DB-backed library index.
- Wired ComfyUI drag-and-drop imports through the same single-target index refresh path.
- Kept this as an exact-path upsert/cache invalidation path, not a filesystem watcher or full selected-folder scan.

### Code And Docs

- Backend: added completion hook registration and guarded notification in `app/downloader.py` and `app/subscriptions.py`, plus completed-job/item library index helpers in `app/main.py`.
- Tests: added completed-download hook coverage, including the file-target-to-parent-card case and subscription download worker indexing.
- Docs: updated README_LLM, architecture, configuration, operations, feature map, and this patch note.

### Verification

- Command/result: `python3 -m py_compile app/main.py app/downloader.py app/subscriptions.py` passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py -k 'completed_download or library_index_scan_populates'` passed, 3 tests.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_youtube_subscriptions.py -k 'download_worker_downloads_ready_items_without_jobs'` passed, 1 test.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py` passed, 105 tests with one existing Starlette deprecation warning.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_youtube_subscriptions.py` passed, 15 tests.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_downloader_runtime.py` passed, 72 tests.
- Command/result: `git diff --check` passed.
- Command/result: `python3 -m py_compile app/main.py app/db.py app/defaults.py app/downloader.py app/internal_jobs.py app/parsers.py app/workflows.py app/metadata.py app/utils.py app/subscriptions.py` passed.
- Command/result: `node --check chrome-extension/shared.js`, `node --check chrome-extension/background.js`, `node --check chrome-extension/popup.js`, and manifest JSON parse passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider` passed, 298 tests with one existing Starlette deprecation warning.

### Deploy Impact

- No operator migration, new environment variable, system package, or watcher service is required.
- Files HugCivi downloads/imports, including subscription downloads, should appear as library cards without pressing `갱신`; files added externally on the NAS still rely on quick sync, background indexing, explicit live mode, or reindex.

### Handoff Notes

- This keeps the LoRA Manager/Plex-style persisted-card metaphor for HugCivi-owned writes while preserving the current explicit reconcile model for external filesystem changes.

## Library Quick Sync Refresh

Time: 11:54 KST
Commit: pending
Status: verified

### Summary

- Changed the library `갱신` flow to use a quick scoped DB sync before any heavier reindex work.
- Added `POST /api/library/sync`, which upserts found cards and marks missing indexed rows stale without deleting the selected folder's existing DB rows first.
- Kept `/api/library/reindex` as the heavier background rebuild path for recovery or incomplete large scopes.
- Updated thumbnail backfill submission so the server-side `MEDIA_THUMBNAIL_BACKFILL_WORKERS` setting is honored unless an API caller explicitly provides workers.

### Code And Docs

- Backend: added scoped sync helpers in `app/main.py` and scoped prune support in `app/db.py`.
- Frontend: changed the library refresh button to call `/api/library/sync` and refresh the DB-backed page without clearing cards into a reindex loading state.
- Tests: added quick sync upsert/prune coverage and updated template assertions.
- Docs: updated README_LLM, architecture, configuration, operations, feature map, media scaling comparison, and this patch note.

### Verification

- Command/result: `python3 -m py_compile app/main.py app/db.py app/defaults.py app/internal_jobs.py` passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py -k 'library or watcher or thumbnail or policy_status or home_template_declares'` passed, 47 tests.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py` passed, 103 tests with one existing Starlette deprecation warning.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_api.py tests/test_transfer_core.py` passed, 49 tests.
- Command/result: `git diff --check` passed.
- Command/result: `python3 -m py_compile app/main.py app/db.py app/defaults.py app/downloader.py app/internal_jobs.py app/parsers.py app/workflows.py app/metadata.py app/utils.py app/subscriptions.py` passed.
- Command/result: `node --check chrome-extension/shared.js && node --check chrome-extension/background.js && node --check chrome-extension/popup.js && node -e "JSON.parse(require('fs').readFileSync('chrome-extension/manifest.json','utf8'))"` passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_civitai_viewer_health.py -k 'library or card'` passed, 2 tests.
- Command/result: `python3 -m pytest -q -p no:cacheprovider` passed, 296 tests with one existing Starlette deprecation warning.

### Deploy Impact

- No operator migration is required. New optional env knobs are `LIBRARY_SYNC_MAX_PATHS` and `LIBRARY_SYNC_PRUNE_LIMIT`.

### Handoff Notes

- This adopts the ComfyUI LoRA Manager-style persisted-cache/reconcile direction without copying GPL code and without claiming real filesystem watcher behavior.
- `LIBRARY_WATCHER_ENABLED` remains policy/status only; external NAS-side changes still rely on quick sync, background indexing, explicit live mode, or full reindex.

### Follow-Up

- Consider a durable dirty-path queue or true local-only filesystem watcher behind the existing watcher policy only if product direction changes.

## Library Card Frontend Render Optimization

Time: afternoon KST
Commit: pending
Status: verified

### Summary

- Coalesced non-blocking library card rerenders into the next `requestAnimationFrame` tick so repeated refresh/job/favorite updates do not rebuild the grid multiple times in one frame.
- Updated ordinary favorite toggles to patch the visible card button state in place when favorite sorting does not require a page refresh.
- Cancelled pending scheduled renders before showing library loading state and unobserved stale thumbnail nodes before replacing card HTML.
- Allowed newly ready thumbnail URLs to retry after a previous deferred image error state.
- Added card-level CSS containment and `content-visibility: auto` with stable intrinsic heights to reduce layout/paint work for offscreen library cards.

### Code And Docs

- Frontend: updated `app/templates/index.html` and `app/static/style.css`.
- Tests: updated template/style assertions in `tests/test_review_fixes.py`.
- Docs: updated `docs/feature-code-map.md`, `docs/operations.md`, and this patch note.

### Verification

- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py -k 'home_template_declares_deferred_thumbnail_queue or home_template_declares_storage_folder_search_ui'` passed, 2 tests.
- Command/result: `git diff --check` passed.
- Command/result: `python3 -m py_compile app/main.py app/db.py app/defaults.py app/downloader.py app/internal_jobs.py app/parsers.py app/workflows.py app/metadata.py app/utils.py app/subscriptions.py` passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py` passed, 103 tests with one existing Starlette deprecation warning.
- Command/result: `python3 -m pytest -q -p no:cacheprovider` passed, 296 tests with one existing Starlette deprecation warning.
- Command/result: `node --check chrome-extension/shared.js && node --check chrome-extension/background.js && node --check chrome-extension/popup.js && node -e "JSON.parse(require('fs').readFileSync('chrome-extension/manifest.json','utf8'))"` passed.
- Command/result: started Uvicorn with temporary `DATA_ROOT`/`DB_PATH`, fetched `/` with Basic Auth and `/static/style.css`, and confirmed the scheduled render and CSS containment markers were served.

### Deploy Impact

- No operator action required. This is a browser-rendering optimization only; library API, DB index, sync, reindex, watcher, and thumbnail cache policies are unchanged.
