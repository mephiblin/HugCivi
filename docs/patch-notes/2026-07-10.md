# 2026-07-10 Patch Notes

## Library Quick Sync Refresh

Time: 11:54 KST
Commit: pending
Status: verified

### Summary

- Changed the library `갱신` flow to use a quick scoped DB sync before any heavier reindex work.
- Added `POST /api/library/sync`, which upserts found cards and marks missing indexed rows stale without deleting the selected folder's existing DB rows first.
- Kept `/api/library/reindex` as the heavier background rebuild path for recovery or incomplete large scopes.
- Updated thumbnail backfill submission so the server-side `MEDIA_THUMBNAIL_BACKFILL_WORKERS` setting is honored unless an API caller explicitly provides workers.

### Code And Docs

- Backend: added scoped sync helpers in `app/main.py` and scoped prune support in `app/db.py`.
- Frontend: changed the library refresh button to call `/api/library/sync` and refresh the DB-backed page without clearing cards into a reindex loading state.
- Tests: added quick sync upsert/prune coverage and updated template assertions.
- Docs: updated README_LLM, architecture, configuration, operations, feature map, media scaling comparison, and this patch note.

### Verification

- Command/result: `python3 -m py_compile app/main.py app/db.py app/defaults.py app/internal_jobs.py` passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py -k 'library or watcher or thumbnail or policy_status or home_template_declares'` passed, 47 tests.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py` passed, 103 tests with one existing Starlette deprecation warning.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_api.py tests/test_transfer_core.py` passed, 49 tests.
- Command/result: `git diff --check` passed.
- Command/result: `python3 -m py_compile app/main.py app/db.py app/defaults.py app/downloader.py app/internal_jobs.py app/parsers.py app/workflows.py app/metadata.py app/utils.py app/subscriptions.py` passed.
- Command/result: `node --check chrome-extension/shared.js && node --check chrome-extension/background.js && node --check chrome-extension/popup.js && node -e "JSON.parse(require('fs').readFileSync('chrome-extension/manifest.json','utf8'))"` passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_civitai_viewer_health.py -k 'library or card'` passed, 2 tests.
- Command/result: `python3 -m pytest -q -p no:cacheprovider` passed, 296 tests with one existing Starlette deprecation warning.

### Deploy Impact

- No operator migration is required. New optional env knobs are `LIBRARY_SYNC_MAX_PATHS` and `LIBRARY_SYNC_PRUNE_LIMIT`.

### Handoff Notes

- This adopts the ComfyUI LoRA Manager-style persisted-cache/reconcile direction without copying GPL code and without claiming real filesystem watcher behavior.
- `LIBRARY_WATCHER_ENABLED` remains policy/status only; external NAS-side changes still rely on quick sync, background indexing, explicit live mode, or full reindex.

### Follow-Up

- Consider a durable dirty-path queue or true local-only filesystem watcher behind the existing watcher policy only if product direction changes.
