# 2026-07-08 Patch Notes

## Selected-Folder Library Page Totals

Time: 13:17 KST
Commit: `pending`
Status: released

### Summary

- Changed selected-folder live library pagination so ordinary completed scans report `total_count` and `total_pages`.
- The library footer can now show the full page list for selected folders, instead of only `이전 / 현재 / 다음`, when the backend knows the exact page count.
- Added completion-aware live scan helpers so incomplete scans do not pretend to know totals.
- Kept the unknown-total fallback for scans that hit the internal path budget.

### Code And Docs

- Backend: updated `live_library_items_page`, added completion-aware live item/path helpers, and kept selected-folder live payloads in `mode="live"`.
- Frontend/addon: no template change needed; existing known-total pagination path is reused.
- Tests: added selected-folder total-page, five-page boundary, short-final-page, and incomplete-scan regressions; updated live pagination return-shape coverage.
- Docs: updated current behavior notes, architecture/configuration/operations references, feature map, and the library performance planning status note.

### Verification

- Command/result: `python3 -m py_compile app/main.py tests/test_review_fixes.py` passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py -k 'selected_folder_live_library_five_page_boundaries or selected_folder_live_library_reports_total_pages or selected_folder_live_library_keeps_unknown_totals'` passed, 3 tests.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py -k 'selected_folder_live_library or library_api_date_sort or live_library_pagination or home_template_declares_storage_folder_search_ui'` passed, 6 tests.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py` passed, 70 tests with one existing Starlette deprecation warning.
- Command/result: `python3 -m pytest -q -p no:cacheprovider` passed, 259 tests with one existing Starlette deprecation warning.

### Deploy Impact

- Published by the local `linux/amd64` image build for this release commit. Portainer/Synology users can pull `ghcr.io/mephiblin/hugcivi:latest` after the image push.

### Handoff Notes

- Known totals are returned only when the selected-folder scan completes within `LIVE_LIBRARY_PAGE_COUNT_MAX_PATHS`.
- If the path budget is hit, `total_count` and `total_pages` remain `null` so the UI keeps the previous/current/next fallback.

### Follow-Up

- If very large selected folders still need exact totals, consider a folder-scoped indexed count instead of raising the live path budget.

## Transfer Review Fixes

Time: 02:30 KST
Commit: `pending`
Status: verified

### Summary

- Added a visible library-card `사용 리소스 전송` action for Civitai image-page archives so the flow is not right-click only.
- Cleared untouched ComfyUI default mappings when the target base path is changed away from a models-root path, preventing duplicated destinations such as `checkpoints/checkpoints`.
- Hardened transfer browsing and job creation: disabled Receiver/local-mount targets cannot browse trees, Receiver tree paths are revalidated before returning to the browser, and Civitai resource transfer jobs are fully validated/created before enqueueing.
- Preserved Civitai resource-transfer metadata when `transfer_copy` jobs start, and tightened local-mount no-clobber finalization for late destination-file races.

### Code And Docs

- Backend: updated transfer tree routes, Receiver tree sanitization, resource batch job creation, transfer metadata merge, and local-mount copy finalization.
- Frontend/addon: added the card action, target-group fallback, modal label structure cleanup, and ComfyUI default-mapping clearing.
- Tests: added transfer tree hardening, resource metadata preservation, template affordance, and local-mount no-clobber coverage.
- Docs: updated configuration, architecture, operations, feature map, and this patch note.

### Verification

- Command/result: `python3 -m py_compile app/main.py app/transfer.py app/db.py app/internal_jobs.py` passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_core.py tests/test_transfer_api.py` passed, 47 tests.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_civitai_viewer_health.py tests/test_review_fixes.py` passed, 78 tests with one existing Starlette deprecation warning.
- Command/result: `awk ... app/templates/index.html | sed ... > /tmp/hugcivi-index-script.js && node --check /tmp/hugcivi-index-script.js` passed.
- Command/result: `git diff --check` passed.

### Deploy Impact

- None until the next app image/build is published.

### Handoff Notes

- Custom ComfyUI mappings are preserved; only the untouched default preset is cleared when the base path stops looking like a ComfyUI models root.

### Follow-Up

- None.

## Civitai Resources Used Transfer

Time: 02:06 KST
Commit: `pending`
Status: verified

### Summary

- Added a Civitai image-card right-click action, `사용 리소스 전송`, for copying locally present Resources used entries to a registered transfer target.
- Added preflight and job APIs that read `_civitai_image_metadata.json`, check model-version resources through existing local job/sidecar health logic, resolve present resources to primary local model files, and queue one `transfer_copy` job per file.
- The flow defaults to the ComfyUI target group and reuses saved `policy.comfyui_mappings`, so files under HugCivi `stable-diffusion/...` routes land in the configured ComfyUI model subfolders.

### Code And Docs

- Backend: added `/api/transfer/civitai-resources/preflight`, `/api/transfer/civitai-resources/jobs`, resource planning helpers, and primary-model-file resolution from Civitai model sidecars.
- Frontend/addon: added the right-click menu item, resource transfer modal, target group selection, preflight summary, and multi-job submit flow.
- Tests: added Civitai resource transfer API coverage and updated transfer/Civitai template affordance checks.
- Docs: updated feature map, architecture, operations, and this patch note.

### Verification

- Command/result: `python3 -m py_compile app/main.py` passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_api.py::test_civitai_resource_transfer_queues_primary_files_with_comfyui_mapping tests/test_transfer_api.py::test_home_template_declares_transfer_ui_without_mode_payload tests/test_civitai_viewer_health.py::test_civitai_image_cards_are_media_archives_before_library_scan` passed, 3 tests.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_api.py tests/test_civitai_viewer_health.py` passed, 33 tests.
- Command/result: `node --check /tmp/hugcivi-index-script.js` passed after replacing Jinja JSON placeholders with test JSON values.
- Command/result: `git diff --check` passed.
- Command/result: `python3 -m py_compile app/main.py app/transfer.py app/db.py app/internal_jobs.py` passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_api.py tests/test_civitai_viewer_health.py tests/test_review_fixes.py` passed, 100 tests with one existing Starlette deprecation warning.
- Command/result: temp-data local uvicorn served `http://127.0.0.1:8088`; authenticated `curl` returned HTTP 200.

### Deploy Impact

- None until the next app image/build is published. Existing transfer targets keep working; ComfyUI resource transfer works best with a ComfyUI category target that has `policy.comfyui_mappings`.

### Handoff Notes

- Resource transfer intentionally sends resolved model files, not the whole Civitai model archive folder, so ComfyUI receives `*.safetensors`/model files directly under mapped model folders.
- Missing Resources used entries are reported in preflight but not queued; duplicate local files are skipped.

### Follow-Up

- None.

## Transfer Settings Category-First UI

Time: 01:15 KST
Commit: `pending`
Status: verified

### Summary

- Changed Settings -> `전송 대상` so category tabs appear first, followed by the selected category's registered targets and registration form.
- Category selection now updates registration defaults for target name/path placeholders and allowed source prefixes.
- New transfer targets store an optional `policy.category` hint for UI grouping while keeping copy behavior unchanged.
- The `/data 전체 복제` tool is scoped to the `종합` category.

### Code And Docs

- Backend: allowed sanitized transfer copy policies to carry optional `category` metadata.
- Frontend/addon: reorganized the transfer settings pane, added category-specific form presets, and made registered-target filtering prefer explicit policy categories for new targets.
- Tests: updated transfer template coverage and added policy-category sanitization coverage.
- Docs: updated feature map, configuration, architecture, and operations references.

### Verification

- Command/result: `python3 -m py_compile app/main.py app/transfer.py` passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_core.py tests/test_transfer_api.py::test_home_template_declares_transfer_ui_without_mode_payload` passed, 22 tests.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_api.py` passed, 20 tests.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_db.py` passed, 7 tests.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py` passed, 67 tests with one existing Starlette deprecation warning.
- Command/result: `node --check chrome-extension/shared.js && node --check chrome-extension/background.js && node --check chrome-extension/popup.js && node -e "JSON.parse(require('fs').readFileSync('chrome-extension/manifest.json','utf8'))"` passed.
- Command/result: `git diff --check` passed.
- Command/result: local uvicorn served `http://127.0.0.1:8088`; authenticated `curl` and headless Chrome DOM checks found the new transfer category panel and presets.

### Deploy Impact

- None until the next app image/build is published. Existing transfer targets continue to load; new targets may include `policy.category` in SQLite.

### Handoff Notes

- Existing targets without `policy.category` still use name/path/prefix inference for category tabs.
- New targets saved from a category tab are pinned to that category for settings-list filtering.

### Follow-Up

- None.

## ComfyUI Transfer Mapping Fields

Time: 01:33 KST
Commit: `pending`
Status: verified

### Summary

- Added ComfyUI-category transfer target mapping fields from fixed HugCivi `stable-diffusion/<route>` sources to user-editable destination subfolders.
- Filled default mappings for ComfyUI models-root targets, including `checkpoints`, `loras`, `diffusion_models`, `vae`, `controlnet`, `embeddings`, and `upscale_models`.
- Applied saved ComfyUI mappings as the default `destination_subpath` when a transfer request does not explicitly choose another destination.

### Code And Docs

- Backend: added sanitized `policy.comfyui_mappings` support and default destination-subpath resolution for transfer requests.
- Frontend/addon: added ComfyUI mapping inputs, mapping summaries, and transfer-modal default destination selection from saved mappings.
- Tests: added transfer API and policy sanitization coverage; updated transfer template regression checks.
- Docs: updated configuration, architecture, operations, and feature map references.

### Verification

- Command/result: `python3 -m py_compile app/main.py app/transfer.py` passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_core.py tests/test_transfer_api.py::test_transfer_preflight_uses_comfyui_mapping_when_destination_is_empty tests/test_transfer_api.py::test_home_template_declares_transfer_ui_without_mode_payload` passed, 24 tests.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_api.py` passed, 21 tests.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_db.py tests/test_review_fixes.py` passed, 74 tests with one existing Starlette deprecation warning.
- Command/result: `node --check chrome-extension/shared.js && node --check chrome-extension/background.js && node --check chrome-extension/popup.js && node -e "JSON.parse(require('fs').readFileSync('chrome-extension/manifest.json','utf8'))"` passed.
- Command/result: `git diff --check` passed.
- Command/result: restarted local uvicorn at `http://127.0.0.1:8088`; authenticated `curl` and headless Chrome DOM checks found the ComfyUI mapping panel, mapping constants, and `comfyui_mappings` payload code.

### Deploy Impact

- None until the next app image/build is published. New ComfyUI targets may include `policy.comfyui_mappings` in SQLite.

### Handoff Notes

- Empty ComfyUI mapping inputs are omitted; explicit transfer `destination_subpath` still wins over stored mappings.
- The source side is intentionally limited to `stable-diffusion/...` prefixes.

### Follow-Up

- None.

## Library Pagination Shows All Known Pages

Time: 01:22 KST
Commit: `pending`
Status: verified

### Summary

- Changed the library pagination footer so indexed library views render every known page number instead of collapsing distant pages behind `...`.
- Live selected-folder scans with unknown total pages keep the existing previous/current/next behavior until the backend reports a known total. Later on 2026-07-08, selected-folder scans gained known totals when the scan completes within the internal path budget.

### Code And Docs

- Backend: none.
- Frontend/addon: added a library-specific known-page list and kept job-list pagination unchanged.
- Tests: updated template regression coverage for the new library pagination behavior.
- Docs: added this patch note.

### Verification

- Command/result: `node --check chrome-extension/shared.js && node --check chrome-extension/background.js && node --check chrome-extension/popup.js && node -e "JSON.parse(require('fs').readFileSync('chrome-extension/manifest.json','utf8'))"` passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py` passed, 67 tests with one existing Starlette deprecation warning.

### Deploy Impact

- None until the next app image/build is published.

### Handoff Notes

- `renderJobsPagination()` still uses the compact numbered window; only `renderLibraryPagination()` expands all known `total_pages`.

### Follow-Up

- None.
