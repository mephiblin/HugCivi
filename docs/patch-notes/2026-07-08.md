# 2026-07-08 Patch Notes

## Transfer Settings Category-First UI

Time: 01:15 KST
Commit: `pending`
Status: verified

### Summary

- Changed Settings -> `전송 대상` so category tabs appear first, followed by the selected category's registered targets and registration form.
- Category selection now updates registration defaults for target name/path placeholders and allowed source prefixes.
- New transfer targets store an optional `policy.category` hint for UI grouping while keeping copy behavior unchanged.
- The `/data 전체 복제` tool is scoped to the `종합` category.

### Code And Docs

- Backend: allowed sanitized transfer copy policies to carry optional `category` metadata.
- Frontend/addon: reorganized the transfer settings pane, added category-specific form presets, and made registered-target filtering prefer explicit policy categories for new targets.
- Tests: updated transfer template coverage and added policy-category sanitization coverage.
- Docs: updated feature map, configuration, architecture, and operations references.

### Verification

- Command/result: `python3 -m py_compile app/main.py app/transfer.py` passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_core.py tests/test_transfer_api.py::test_home_template_declares_transfer_ui_without_mode_payload` passed, 22 tests.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_api.py` passed, 20 tests.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_db.py` passed, 7 tests.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py` passed, 67 tests with one existing Starlette deprecation warning.
- Command/result: `node --check chrome-extension/shared.js && node --check chrome-extension/background.js && node --check chrome-extension/popup.js && node -e "JSON.parse(require('fs').readFileSync('chrome-extension/manifest.json','utf8'))"` passed.
- Command/result: `git diff --check` passed.
- Command/result: local uvicorn served `http://127.0.0.1:8088`; authenticated `curl` and headless Chrome DOM checks found the new transfer category panel and presets.

### Deploy Impact

- None until the next app image/build is published. Existing transfer targets continue to load; new targets may include `policy.category` in SQLite.

### Handoff Notes

- Existing targets without `policy.category` still use name/path/prefix inference for category tabs.
- New targets saved from a category tab are pinned to that category for settings-list filtering.

### Follow-Up

- None.

## ComfyUI Transfer Mapping Fields

Time: 01:33 KST
Commit: `pending`
Status: verified

### Summary

- Added ComfyUI-category transfer target mapping fields from fixed HugCivi `stable-diffusion/<route>` sources to user-editable destination subfolders.
- Filled default mappings for ComfyUI models-root targets, including `checkpoints`, `loras`, `diffusion_models`, `vae`, `controlnet`, `embeddings`, and `upscale_models`.
- Applied saved ComfyUI mappings as the default `destination_subpath` when a transfer request does not explicitly choose another destination.

### Code And Docs

- Backend: added sanitized `policy.comfyui_mappings` support and default destination-subpath resolution for transfer requests.
- Frontend/addon: added ComfyUI mapping inputs, mapping summaries, and transfer-modal default destination selection from saved mappings.
- Tests: added transfer API and policy sanitization coverage; updated transfer template regression checks.
- Docs: updated configuration, architecture, operations, and feature map references.

### Verification

- Command/result: `python3 -m py_compile app/main.py app/transfer.py` passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_core.py tests/test_transfer_api.py::test_transfer_preflight_uses_comfyui_mapping_when_destination_is_empty tests/test_transfer_api.py::test_home_template_declares_transfer_ui_without_mode_payload` passed, 24 tests.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_api.py` passed, 21 tests.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_db.py tests/test_review_fixes.py` passed, 74 tests with one existing Starlette deprecation warning.
- Command/result: `node --check chrome-extension/shared.js && node --check chrome-extension/background.js && node --check chrome-extension/popup.js && node -e "JSON.parse(require('fs').readFileSync('chrome-extension/manifest.json','utf8'))"` passed.
- Command/result: `git diff --check` passed.
- Command/result: restarted local uvicorn at `http://127.0.0.1:8088`; authenticated `curl` and headless Chrome DOM checks found the ComfyUI mapping panel, mapping constants, and `comfyui_mappings` payload code.

### Deploy Impact

- None until the next app image/build is published. New ComfyUI targets may include `policy.comfyui_mappings` in SQLite.

### Handoff Notes

- Empty ComfyUI mapping inputs are omitted; explicit transfer `destination_subpath` still wins over stored mappings.
- The source side is intentionally limited to `stable-diffusion/...` prefixes.

### Follow-Up

- None.

## Library Pagination Shows All Known Pages

Time: 01:22 KST
Commit: `pending`
Status: verified

### Summary

- Changed the library pagination footer so indexed library views render every known page number instead of collapsing distant pages behind `...`.
- Live selected-folder scans with unknown total pages keep the existing previous/current/next behavior until the backend reports a known total.

### Code And Docs

- Backend: none.
- Frontend/addon: added a library-specific known-page list and kept job-list pagination unchanged.
- Tests: updated template regression coverage for the new library pagination behavior.
- Docs: added this patch note.

### Verification

- Command/result: `node --check chrome-extension/shared.js && node --check chrome-extension/background.js && node --check chrome-extension/popup.js && node -e "JSON.parse(require('fs').readFileSync('chrome-extension/manifest.json','utf8'))"` passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py` passed, 67 tests with one existing Starlette deprecation warning.

### Deploy Impact

- None until the next app image/build is published.

### Handoff Notes

- `renderJobsPagination()` still uses the compact numbered window; only `renderLibraryPagination()` expands all known `total_pages`.

### Follow-Up

- None.
