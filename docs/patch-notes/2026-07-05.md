# Text And Markdown Document Cards

Commits: `7e7d417`
Status: verified

## Summary

- `.txt`, `.md`, and `.markdown` files are now treated as readable document media.
- Document-only folders can appear as library cards, and mixed archives such as ASMR.one works can show text files in the media strip next to audio/images.
- `/api/media/text` reads document previews with a 512KB cap and returns JSON with `truncated`, `encoding`, and size metadata.
- The frontend renders Markdown as escaped text, not HTML, to avoid XSS and sanitizer complexity.
- The ambiguous top-right global refresh icon was removed; automatic polling and the storage-folder refresh button cover the remaining refresh cases.
- The top-right controls now keep storage readout, usage calculation, addon download, and thumbnail blur in that order.
- The main job list now shows an always-available `ALL` filter plus source filters for providers currently present in job history.

## Code And Docs

- Backend: document file detection, media payloads, safe text preview API in `app/main.py`.
- Frontend: document normalization, viewer loading, document strip labels, and scrollable escaped text panel in `app/templates/index.html` and `app/static/style.css`.
- Tests: document media/card/API coverage in `tests/test_review_fixes.py`.
- Docs: updated `README.md`, `README_LLM.md`, `PATCH_NOTES.md`, `docs/architecture.md`, `docs/development.md`, `docs/feature-code-map.md`, and this note.

## Verification

- `python3 -m py_compile app/main.py app/downloader.py`: passed.
- `awk '/<script>/{flag=1; next} /<\/script>/{flag=0} flag {print}' app/templates/index.html | sed -e 's/^    const initialJobs = .*/    const initialJobs = [];/' -e 's/^    const initialJobsPage = .*/    const initialJobsPage = {};/' -e 's/^    const initialLibraryItems = .*/    const initialLibraryItems = [];/' -e 's/^    const initialStorage = .*/    const initialStorage = {};/' | node --check`: passed.
- `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py tests/test_asmrone_provider.py`: 63 passed, 1 existing Starlette template deprecation warning.
- `python3 -m pytest -q -p no:cacheprovider`: 186 passed, 1 existing Starlette template deprecation warning.
- `git diff --check`: passed.

## Deploy Impact

- Rebuild and push `ghcr.io/mephiblin/hugcivi:latest` so Portainer/Synology deployments receive the job source filters, document media viewer, and top-control cleanup.

## Handoff Notes

- The main job list still uses 50-row numbered pagination. `source_counts` drives the provider filter buttons and `source=<provider>` filters rows server-side.

## Library Card Performance Plan

Time: 01:33 KST
Commit: `079d53f`
Status: planning

### Summary

- Added `docs/library-card-performance-plan-2026-07-05.md` as the current plan for slow library card rendering and startup/refresh pressure.
- The plan covers server-side library pagination, thumbnail caching, avoiding unnecessary rerenders during job polling, and API compatibility for existing consumers.

### Code And Docs

- Docs: added the library card performance plan and linked it from `docs/index.md`.

### Verification

- Docs-only plan. No runtime code changed in `079d53f`.

### Deploy Impact

- None by itself. The plan does not change runtime behavior until implemented.

### Handoff Notes

- Treat the plan as proposed work, not current implementation. Current library behavior remains the existing full-card payload/render path.

### Follow-Up

- Implement the plan in separate backend/frontend/test commits before claiming runtime performance improvement.
