## Transfer Settings UX Split

Time: 2026-07-07 KST
Commit: `aebc6e4`
Status: verified

### Summary

- Split the transfer settings pane into separate areas for connected-folder target creation, `/data` root clone jobs, and registered target management.
- Moved Receiver/rclone target fields behind an advanced disclosure so the normal Portainer `/data_remote` flow is the first visible path.
- Changed the connected-folder defaults to allow common archive roots and leave include patterns empty, so images and mixed folders are not silently filtered out by model-only defaults.

### Deploy Impact

- No schema or environment changes. Existing transfer targets remain compatible; this only changes the settings-pane layout and local mount defaults for newly created targets.

### Verification

- Command/result: `git diff --check` passed.
- Command/result: rendered `app/templates/index.html` inline script `node --check` passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_api.py::test_home_template_declares_transfer_ui_without_mode_payload` passed, 1 passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_api.py tests/test_transfer_core.py tests/test_transfer_db.py` passed, 37 passed.
- Command/result: Chrome extension `node --check` and manifest JSON parse passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py` passed, 67 passed / 1 warning.
- Command/result: backend `py_compile` baseline passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider` passed, 239 passed / 1 warning.

## Transfer Settings Save Debug

Time: 2026-07-07 KST
Commit: `pending`
Status: verified

### Summary

- Reproduced the transfer settings save flow in a real browser with an isolated temporary DB and confirmed the dedicated `연결 폴더 저장` button persisted targets.
- Fixed the confusing path where the transfer pane's top/bottom general `저장` buttons submitted `/settings` and ignored the transfer target fields.
- Kept the transfer target save success message visible after the target list refresh, so users get clear feedback that the input was stored.

### Code And Docs

- Frontend: `app/templates/index.html` now intercepts settings form submit while the transfer pane is active and calls `saveTransferSettingTarget()`.
- Frontend: `loadTransferSettingTargets()` accepts a success status option so save feedback is not immediately cleared.
- Tests: `tests/test_transfer_api.py` covers the submit interception and success-status preservation hooks.
- Docs: `PATCH_NOTES.md`, `docs/feature-code-map.md`, and this handoff note document the save behavior.

### Verification

- Command/result: Playwright browser reproduction against `http://127.0.0.1:8765` with temp `DATA_ROOT`, `DB_PATH`, and `DATA_REMOTE_DIR` confirmed both `연결 폴더 저장` and footer `저장` create visible transfer targets.
- Command/result: `git diff --check` passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_api.py::test_home_template_declares_transfer_ui_without_mode_payload` passed, 1 passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_api.py tests/test_transfer_core.py tests/test_transfer_db.py` passed, 37 passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py` passed, 67 passed / 1 warning.
- Command/result: `python3 -m pytest -q -p no:cacheprovider` passed, 239 passed / 1 warning.

### Deploy Impact

- No schema or environment changes. Existing targets remain compatible; only settings-pane submit behavior and feedback changed.

### Handoff Notes

- The transfer target fields intentionally remain outside the normal `/settings` form payload. The transfer pane intercepts normal settings submit buttons and writes targets through `/api/transfer/targets`.

### Follow-Up

- None.

## Transfer Target Group Filters

Time: 2026-07-07 KST
Commit: `pending`
Status: verified

### Summary

- Added category filters to the transfer modal target selector and the settings-pane registered target list.
- The groups are `종합`, `ComfyUI`, `Hugging Face`, `Civitai`, `Hitomi`, `Movie`, and `ASMR`.
- Targets are grouped from their saved name, type, remote/base path, allowed source prefixes, and include patterns, so existing targets do not need a DB migration.

### Code And Docs

- Frontend: `app/templates/index.html` renders shared target-group segmented controls for the modal and settings list.
- Frontend: transfer target filtering reuses `allowed_source_prefixes` plus target metadata to match source/provider groups.
- Styles: `app/static/style.css` adds compact grouped target buttons and empty states.
- Tests: `tests/test_transfer_api.py` covers the group controls and matching helpers in the home template.
- Docs: `PATCH_NOTES.md`, `docs/feature-code-map.md`, and this handoff note document the grouped transfer lists.

### Verification

- Command/result: Playwright browser reproduction against `http://127.0.0.1:8766` with temp `DATA_ROOT`, `DB_PATH`, and `DATA_REMOTE_DIR` confirmed settings-list grouping and transfer-modal select grouping for ComfyUI, Hugging Face, and Movie targets.
- Command/result: Desktop and mobile Playwright screenshots confirmed grouped target buttons wrap cleanly and mobile delete buttons stay on one line.
- Command/result: `git diff --check` passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_api.py::test_home_template_declares_transfer_ui_without_mode_payload` passed, 1 passed.
- Command/result: backend `py_compile` baseline passed.
- Command/result: Chrome extension `node --check` and manifest JSON parse passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_api.py tests/test_transfer_core.py tests/test_transfer_db.py` passed, 37 passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py` passed, 67 passed / 1 warning.
- Command/result: `python3 -m pytest -q -p no:cacheprovider` passed, 239 passed / 1 warning.

### Deploy Impact

- No schema or environment changes. Existing transfer targets are classified client-side from already stored fields.

### Handoff Notes

- `Movie` currently matches movie/video/youtube/yt-dlp/gallery-dl naming and prefixes. If a future dedicated provider root is added, add its token to `TRANSFER_TARGET_GROUPS`.

### Follow-Up

- None.

## ComfyUI Transfer Folder Check

Time: 2026-07-07 KST
Commit: `pending`
Status: verified

### Summary

- Added a planning document for checking whether a `local_mount` transfer target points at a ComfyUI `models` root or a single ComfyUI model folder.
- Defined the no-structure-change rule for HugCivi `/data`: existing archive routes stay as-is, and ComfyUI folder logic applies only when choosing/checking transfer destinations.
- Documented the initial HugCivi-to-ComfyUI mapping, including `checkpoints`, `loras`, `diffusion_models`, `vae`, `controlnet`, `embeddings`, and `upscale_models`.
- Implemented the folder check core helper, API endpoint, settings-pane `폴더 체크` button, and inline result panel.
- Recognized ComfyUI compatibility folders `clip`, `unet`, and `t2i_adapter` as aliases for `text_encoders`, `diffusion_models`, and `controlnet`.

### Code And Docs

- Backend: `app/transfer.py` now exposes ComfyUI folder constants and `check_comfyui_local_mount_target()`.
- API: `app/main.py` adds `POST /api/transfer/targets/{target_id}/comfyui/check` for enabled `local_mount` targets only.
- Frontend: `app/templates/index.html` and `app/static/style.css` add the settings-pane `폴더 체크` control and compact result rendering.
- Tests: `tests/test_transfer_core.py` and `tests/test_transfer_api.py` cover folder detection, aliases, endpoint restrictions, no absolute path leakage, and template hooks.
- Docs: `docs/comfyui-transfer-folder-check-design-2026-07-07.md` captures the development plan, official ComfyUI folder baseline, API/UI shape, safety rules, and tests.
- Docs: `PATCH_NOTES.md`, `docs/index.md`, `docs/feature-code-map.md`, `docs/configuration.md`, and `docs/architecture.md` document the implemented baseline.

### Verification

- Command/result: `git diff --check` passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_core.py` passed, 20 passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_api.py` passed, 20 passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_transfer_api.py tests/test_transfer_core.py tests/test_transfer_db.py` passed, 47 passed.
- Command/result: `python3 -m py_compile app/main.py app/transfer.py app/db.py app/internal_jobs.py` passed.
- Command/result: rendered `app/templates/index.html` inline script `node --check` passed.
- Command/result: Chrome extension `node --check` and manifest JSON parse passed.
- Command/result: `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py` passed, 67 passed / 1 warning.
- Command/result: Playwright browser checks against `http://127.0.0.1:8767` with temp `DATA_ROOT`, `DB_PATH`, and `DATA_REMOTE_DIR` confirmed the ComfyUI folder check endpoint and settings-pane UI on desktop and mobile.
- Command/result: `python3 -m pytest -q -p no:cacheprovider` passed, 249 passed / 1 warning.

### Deploy Impact

- No schema, setting, or `/data` layout changes. Existing transfer targets remain compatible. The new API only reads enabled `local_mount` targets under `DATA_REMOTE_DIR` and does not create folders.

### Follow-Up

- Use the check result to suggest or auto-fill `destination_subpath` in the transfer modal.
