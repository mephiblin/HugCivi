## Civitai Resources Used ComfyUI Folder Routing

Time: 00:35 KST
Commit: `pending`
Status: verified

### Summary

- Civitai image-card `사용 리소스 전송` now routes locally present resources to their standard ComfyUI model subfolders when the selected target is rooted at `ComfyUI/models` but has no saved mappings.
- Existing saved custom mappings remain authoritative.
- The fallback covers checkpoints, LoRAs, diffusion models, VAEs, ControlNet models, embeddings, and upscalers.

### Code And Docs

- Backend: added standard route lookup and models-root detection helpers in `app/transfer.py`, then applied them only to unmapped Civitai resource transfer planning in `app/main.py`.
- Frontend/addon: no change.
- Tests: added route-table and models-root detection coverage, and changed the Civitai resource API test to prove fallback routing without saved mappings.
- Docs: updated README, architecture, configuration, feature/code map, and root patch notes.

### Verification

- Command/result: `python -m pytest -q -p no:cacheprovider tests/test_transfer_core.py tests/test_transfer_api.py tests/test_transfer_db.py` passed, 71 tests.
- Command/result: Python compile checks, Chrome extension JavaScript syntax checks, manifest JSON parsing, and `git diff --check` passed.
- Command/result: full suite completed with 316 passed and 2 failed. Both failures (`test_media_thumbnail_backfill_job_uses_card_representatives` and `test_text_and_markdown_files_are_readable_media_cards`) reproduce unchanged on clean `origin/main`; the user explicitly accepted these unrelated baseline failures before release.

### Deploy Impact

- A new `linux/amd64` GHCR image will be published after full verification.
- No DB migration or settings rewrite is required; existing targets benefit automatically when their base path clearly identifies a ComfyUI models root.

### Handoff Notes

- Fallback routing is intentionally limited to Civitai Resources used transfer. Normal transfers continue to use saved mappings or an explicitly selected destination.
- A target rooted at a single model folder such as `ComfyUI/models/loras` is not treated as a models root.

### Follow-Up

- None.

## Floating Transfer Queue And Device-Prefixed Models Root

Time: 00:58 KST
Commit: `pending`
Status: verified

### Summary

- Added a bottom-right transfer status control with an active-job badge and an active-first/recent job popover.
- The popover shows source and destination subpaths, progress, localized status, log links, and a jump to the existing Transfer-filtered jobs table.
- Fixed Civitai Resources used fallback routing for device-prefixed share names such as `5060TI_ComfyUI-models`; LoRAs now resolve under `loras/...` instead of the target root.

### Code And Docs

- Backend: decorated `transfer_copy` job summaries with sanitized source path, destination subpath, and root-clone display fields; expanded ComfyUI models-root basename recognition.
- Frontend/addon: added the responsive transfer rail button/popover, independent transfer polling, active badge, and full-list navigation; aligned the settings-form models-root detector with the backend.
- Tests: extended transfer API/template coverage and added device-prefixed models-root cases.
- Docs: updated README, architecture, configuration, feature/code map, and patch notes.

### Verification

- Command/result: transfer core/API targeted checks passed, 44 tests; all transfer suites passed, 74 tests.
- Command/result: full suite completed with 319 passed and 2 failed. Both failures (`test_media_thumbnail_backfill_job_uses_card_representatives` and `test_text_and_markdown_files_are_readable_media_cards`) are the previously accepted, unrelated baseline failures.
- Command/result: Python compile checks, rendered inline-script parsing, Chrome extension JavaScript/manifest checks, and `git diff --check` passed.
- Visual QA: desktop 1440×757 and mobile 390×844 rendered with populated transfer rows; no horizontal overflow or mobile-tab overlap. Pointer interaction verified close, reopen, and full Transfer-list navigation.

### Deploy Impact

- A new application image is required before the queue UI and expanded models-root detection are available in production.
- Files already copied to the target models root are not moved automatically.

### Handoff Notes

- The floating queue uses the existing `/api/jobs` polling contract and does not add a new endpoint.
- The models-root detector accepts a basename ending in a separated `comfyui-models` form while rejecting unrelated names such as `mycomfyui-models`.

### Follow-Up

- None.
