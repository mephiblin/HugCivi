## Civitai Resources Used ComfyUI Folder Routing

Time: 00:35 KST
Commit: `pending`
Status: verified

### Summary

- Civitai image-card `사용 리소스 전송` now routes locally present resources to their standard ComfyUI model subfolders when the selected target is rooted at `ComfyUI/models` but has no saved mappings.
- Existing saved custom mappings remain authoritative.
- The fallback covers checkpoints, LoRAs, diffusion models, VAEs, ControlNet models, embeddings, and upscalers.

### Code And Docs

- Backend: added standard route lookup and models-root detection helpers in `app/transfer.py`, then applied them only to unmapped Civitai resource transfer planning in `app/main.py`.
- Frontend/addon: no change.
- Tests: added route-table and models-root detection coverage, and changed the Civitai resource API test to prove fallback routing without saved mappings.
- Docs: updated README, architecture, configuration, feature/code map, and root patch notes.

### Verification

- Command/result: `python -m pytest -q -p no:cacheprovider tests/test_transfer_core.py tests/test_transfer_api.py tests/test_transfer_db.py` passed, 71 tests.
- Command/result: Python compile checks, Chrome extension JavaScript syntax checks, manifest JSON parsing, and `git diff --check` passed.
- Command/result: full suite completed with 316 passed and 2 failed. Both failures (`test_media_thumbnail_backfill_job_uses_card_representatives` and `test_text_and_markdown_files_are_readable_media_cards`) reproduce unchanged on clean `origin/main`; the user explicitly accepted these unrelated baseline failures before release.

### Deploy Impact

- A new `linux/amd64` GHCR image will be published after full verification.
- No DB migration or settings rewrite is required; existing targets benefit automatically when their base path clearly identifies a ComfyUI models root.

### Handoff Notes

- Fallback routing is intentionally limited to Civitai Resources used transfer. Normal transfers continue to use saved mappings or an explicitly selected destination.
- A target rooted at a single model folder such as `ComfyUI/models/loras` is not treated as a models root.

### Follow-Up

- None.
