## Install Guide ByeDPI Proxy Cross-Links

Time: 22:12 KST
Commit: pending
Status: implemented

### Summary

- Added Ubuntu and CasaOS install guide notes that the current discovered `tazihad/byedpi` SOCKS5 proxy can be used with `YT_DLP_PROXY=socks5://192.168.200.100:1080`.
- Linked both install guides to `docs/byedpi-socks-proxy.md` for Portainer stack and Docker CLI proxy install examples.

### Code And Docs

- Backend/frontend: None.
- Docs: Updated `docs/install-ubuntu.md`, `docs/install-casaos.md`, `PATCH_NOTES.md`, and this handoff note.

### Verification

- `git diff --check`: passed.

### Deploy Impact

- Docs-only. No HugCivi image rebuild is required.

### Handoff Notes

- The install guides keep the same limit as the main guide: `YT_DLP_PROXY` is yt-dlp-only and is not whole-container proxying.

### Follow-Up

- None.

## ByeDPI SOCKS5 Proxy Documentation

Time: 22:08 KST
Commit: `ae28b84`
Status: implemented

### Summary

- Identified the current host's DPI-bypass proxy container as `byedpi` using image `tazihad/byedpi`.
- Documented the discovered command, port binding, Portainer stack recreation example, Docker CLI equivalent, and HugCivi `YT_DLP_PROXY` setup with example URL `socks5://192.168.200.100:1080`.

### Code And Docs

- Backend/frontend: None.
- Docs: Added `docs/byedpi-socks-proxy.md` and linked it from `README.md`, `docs/index.md`, `docs/operations.md`, and `PATCH_NOTES.md`.

### Verification

- `docker ps -a`: found `byedpi` from `tazihad/byedpi` with `192.168.200.100:1080->1080/tcp`.
- `docker inspect byedpi`: confirmed entrypoint `/opt/byedpi/ciadpi`, command `--disorder 1 --fake 0 --ttl 1 --auto=torst --tlsrec 1+s`, no restart policy, and no Portainer stack labels.
- Docker Hub/GitHub source check: confirmed the image documents SOCKS5 usage on port `1080`.

### Deploy Impact

- Docs-only. No HugCivi image rebuild is required.
- Installing or changing the ByeDPI proxy container is an operator action outside the HugCivi container.

### Handoff Notes

- `YT_DLP_PROXY` only affects yt-dlp-backed downloads and metadata probes. It does not proxy Hugging Face, Civitai, ASMR.one, generic HTTP, native Hitomi, or internal jobs.
- The guide binds the proxy to `192.168.200.100` rather than `0.0.0.0` to avoid exposing an unauthenticated SOCKS5 proxy beyond the intended LAN IP.

### Follow-Up

- None.

## Documentation Status Audit

Time: 22:03 KST
Commit: `ae28b84`
Status: verified

### Summary

- Audited current README, changelog, reference docs, operations docs, and repo-local skill docs with three Codex workers.
- Clarified that ASMR.one downloads save every leaf track file with `mediaDownloadUrl?action=download`, while audio files are the part directly playable in the media viewer.
- Corrected deployment status: local `main` includes ASMR.one commits, but GHCR `latest` still points to the earlier `sha-625957b` image.

### Code And Docs

- Backend/frontend: None.
- Docs: Updated `README.md`, `PATCH_NOTES.md`, `README_LLM.md`, `docs/architecture.md`, `docs/configuration.md`, `docs/feature-code-map.md`, `docs/operations.md`, this handoff note, and provider/build/frontend skill docs.

### Verification

- `git diff --check`: passed.
- `python3 -m py_compile app/main.py app/db.py app/downloader.py app/internal_jobs.py app/parsers.py app/workflows.py app/metadata.py app/utils.py app/subscriptions.py`: passed.
- `git rev-parse --short origin/main`: `112ed0b`.
- `docker buildx imagetools inspect ghcr.io/mephiblin/hugcivi:latest`: digest `sha256:5708dffc57beb83f56870c43b586df9d00ab2897e23a03329c194d54b712317b`.
- `docker buildx imagetools inspect ghcr.io/mephiblin/hugcivi:sha-625957b`: same digest as `latest`.
- `docker buildx imagetools inspect ghcr.io/mephiblin/hugcivi:sha-6107ece`: not found.
- `docker buildx imagetools inspect ghcr.io/mephiblin/hugcivi:sha-b5c60f1`: not found.

### Deploy Impact

- Docs-only. No image rebuild is caused by this audit.
- Portainer/Synology still needs a new image push before ASMR.one downloads are available from `latest` or a pinned `sha-*` image.

### Handoff Notes

- Keep `README.md` deployment notes human-facing and update them again after the next image push.
- Avoid describing ASMR.one as audio-only; the downloader saves every downloadable leaf track file, while only recognized media types become playable media viewer items.

### Follow-Up

- Build/push a new image and push git when the user wants ASMR.one available in Portainer/Synology.

## ASMR.one Media Body Downloads

Time: 21:32 KST
Commit: `6107ece`
Status: verified

### Summary

- Added ASMR.one work URL support for `/work/RJ...` and `/work/<id>/DLSITE/RJ...` inputs.
- Changed the ASMR.one handler to download actual track files through `mediaDownloadUrl?action=download` instead of saving manifest-only records.
- Added audio media recognition so downloaded ASMR.one archives remain visible in the library and can open in the media viewer.

### Code And Docs

- Backend: Added `asmrone` parser/model fields, provider dispatch, API metadata fetch, folder-safe track file downloads, sidecars, and aggregate progress updates in `app/parsers.py`, `app/models.py`, `app/downloader.py`, and `app/main.py`.
- Frontend/addon: Added basic audio media viewer rendering in `app/templates/index.html` and `app/static/style.css`.
- Tests: Added `tests/test_asmrone_provider.py` for parsing, source URL display, mediaDownloadUrl body download behavior, sidecars, and audio media recognition.
- Docs: Updated `README.md`, `PATCH_NOTES.md`, `docs/architecture.md`, `docs/configuration.md`, `docs/feature-code-map.md`, and the provider skill routing docs.

### Verification

- `git diff --check`: passed.
- `python3 -m py_compile app/main.py app/db.py app/downloader.py app/internal_jobs.py app/parsers.py app/workflows.py app/metadata.py app/utils.py app/subscriptions.py`: passed.
- `node --check chrome-extension/shared.js && node --check chrome-extension/background.js && node --check chrome-extension/popup.js && node -e "JSON.parse(require('fs').readFileSync('chrome-extension/manifest.json','utf8'))"`: passed.
- `python3 -m pytest -q -p no:cacheprovider`: 165 passed, 1 existing Starlette template deprecation warning.

### Deploy Impact

- Requires a new container image before Portainer/Synology deployments can use ASMR.one downloads.
- As of this audit, local `main` is ahead of `origin/main` by ASMR.one commits `6107ece` and `b5c60f1`; `origin/main` is `112ed0b`.
- `docker buildx imagetools inspect` shows `ghcr.io/mephiblin/hugcivi:latest` and `ghcr.io/mephiblin/hugcivi:sha-625957b` share digest `sha256:5708dffc57beb83f56870c43b586df9d00ab2897e23a03329c194d54b712317b`.
- `ghcr.io/mephiblin/hugcivi:sha-6107ece` and `ghcr.io/mephiblin/hugcivi:sha-b5c60f1` were not found, so the default Portainer/Synology `latest` image does not include ASMR.one downloads yet.
- New optional environment override: `ASMRONE_API_BASE`, defaulting to `https://api.asmr.one/api`.

### Handoff Notes

- The ASMR.one downloader intentionally uses `mediaDownloadUrl` with `action=download`; it does not persist or request `mediaStreamUrl`.
- Sidecars are written after successful file downloads so filesystem-recovered library cards have source, title, file count, and work metadata.

### Follow-Up

- None.

## Installer Prompt Preflight And Consent

Time: 16:59 KST
Commit: pending
Status: verified

### Summary

- Added `0. 시작 프롬프트: 점검 질문과 설치 허가` sections to `UbuntuPortainer설명서.md` and `CasaOs설명서.md`.
- The prompts require Codex to ask target-system, data-path, port, secret, image-tag, and deployment-mode questions before any inspection or install work.
- Separated read-only inspection approval from install/change approval, with explicit guardrails around Docker/Portainer installs, Stack deployment, folder creation, permission changes, and deletion/volume cleanup.

### Code And Docs

- Backend/frontend: None.
- Docs: Updated the two root installation operation guides plus `PATCH_NOTES.md` and this handoff note.

### Verification

- `rg -n "시작 프롬프트|점검 진행|설치 진행|두 번째 허가|CasaOS Custom Install" UbuntuPortainer설명서.md CasaOs설명서.md`: passed.
- `git diff --check`: passed.

### Deploy Impact

- Docs-only. No image rebuild or Portainer redeploy is required.

### Handoff Notes

- These prompts intentionally make inspection approval and mutation approval separate steps so target-user consent is captured before any server changes.

### Follow-Up

- None.

## Local Image Push For YouTube Fixes

Time: 16:50 KST
Commit: `625957b`
Status: verified

### Summary

- Built and pushed the local `linux/amd64` GHCR image after the YouTube subtitle tolerance and card-title fixes.
- Pushed `origin/main` through `625957b`.
- Confirmed the worktree was clean after the image and git pushes.

### Code And Docs

- Backend/frontend: None beyond the already committed fixes in `747c92f` and `625957b`.
- Docs: Updated `README.md`, `PATCH_NOTES.md`, `docs/operations.md`, `docs/architecture.md`, `docs/feature-code-map.md`, and this handoff note to reflect the current deployed image state.

### Verification

- `git diff --check`: passed before build.
- `python3 -m py_compile app/main.py app/db.py app/downloader.py app/internal_jobs.py app/parsers.py app/workflows.py app/metadata.py app/utils.py app/subscriptions.py`: passed before build.
- `node --check chrome-extension/shared.js`: passed before build.
- `node --check chrome-extension/background.js`: passed before build.
- `node --check chrome-extension/popup.js`: passed before build.
- `node -e "JSON.parse(require('fs').readFileSync('chrome-extension/manifest.json','utf8'))"`: passed before build.
- `python3 -m pytest -q -p no:cacheprovider`: 160 passed, 1 existing Starlette template deprecation warning.
- `/home/inri/.codex/skills/hugcivi-local-image-push/scripts/local-image-push.sh --push`: pushed `ghcr.io/mephiblin/hugcivi:sha-625957b` and `ghcr.io/mephiblin/hugcivi:latest`.
- `git push origin main`: pushed `16e640b..625957b`.

### Deploy Impact

- Portainer/Synology can pull `ghcr.io/mephiblin/hugcivi:latest` for the current deployed image.
- Operators can pin `ghcr.io/mephiblin/hugcivi:sha-625957b` if they want this exact build.

### Handoff Notes

- This release includes `16e640b` scoped folder search, `747c92f` YouTube subtitle 429 tolerance, and `625957b` yt-dlp card title recovery.

### Follow-Up

- None.

## YouTube Card Titles From yt-dlp Info

Time: 12:01 KST
Commit: `625957b`
Status: verified

### Summary

- Changed single-video YouTube/yt-dlp library cards to prefer the real video title from the saved `.info.json` sidecar instead of fallback folder slugs such as `video-...`.
- Applied the same title recovery to decorated completed job rows and filesystem/index-backed library cards so the UI stays consistent whether the DB job row still exists or the card is restored from disk.
- Used the `.info.json` webpage URL as the card source URL when archive metadata only has a `ytdl:` wrapper.

### Code And Docs

- Backend/UI payload: Added yt-dlp info title helpers in `app/main.py`.
- Tests: Added regression coverage for filesystem library cards and decorated gallery-dl job rows in `tests/test_review_fixes.py`.
- Docs: Updated `README.md`, `PATCH_NOTES.md`, and `docs/feature-code-map.md`.

### Verification

- `git diff --check`: passed.
- `python3 -m py_compile app/main.py app/db.py app/downloader.py app/internal_jobs.py app/parsers.py app/workflows.py app/metadata.py app/utils.py app/subscriptions.py`: passed.
- `node --check chrome-extension/shared.js`: passed.
- `node --check chrome-extension/background.js`: passed.
- `node --check chrome-extension/popup.js`: passed.
- `node -e "JSON.parse(require('fs').readFileSync('chrome-extension/manifest.json','utf8'))"`: passed.
- `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py`: 49 passed, 1 existing Starlette template deprecation warning.
- `python3 -m pytest -q -p no:cacheprovider`: 160 passed, 1 existing Starlette template deprecation warning.

### Deploy Impact

- Included in `ghcr.io/mephiblin/hugcivi:sha-625957b` and `ghcr.io/mephiblin/hugcivi:latest`.

### Handoff Notes

- The title override is intentionally limited to single-media yt-dlp/gallery-dl archives so multi-video channel or playlist folders do not get renamed after the first video.

### Follow-Up

- None.

## YouTube Subtitle 429 Tolerance

Time: 11:30 KST
Commit: `747c92f`
Status: verified

### Summary

- Made HugCivi's default YouTube subtitle download options best-effort so subtitle-only 429 failures do not fail an otherwise saved video.
- Kept explicit user subtitle and error-handling yt-dlp options authoritative.
- Excluded `.srt` and `.vtt` sidecars from gallery/yt-dlp media completion checks so subtitle-only partial results cannot be marked as successful downloads.

### Code And Docs

- Backend: Updated `app/downloader.py` yt-dlp command construction and gallery archive media-file detection.
- Tests: Added downloader runtime coverage for best-effort subtitle flags and subtitle sidecar filtering, and reloaded the downloader in YouTube subscription fixtures so `DATA_ROOT` follows each test environment.
- Docs: Updated `README.md`, `PATCH_NOTES.md`, `docs/operations.md`, `docs/configuration.md`, and `docs/feature-code-map.md`.

### Verification

- `git diff --check`: passed.
- `python3 -m py_compile app/main.py app/db.py app/downloader.py app/internal_jobs.py app/parsers.py app/workflows.py app/metadata.py app/utils.py app/subscriptions.py`: passed.
- `node --check chrome-extension/shared.js`: passed.
- `node --check chrome-extension/background.js`: passed.
- `node --check chrome-extension/popup.js`: passed.
- `node -e "JSON.parse(require('fs').readFileSync('chrome-extension/manifest.json','utf8'))"`: passed.
- `python3 -m pytest -q -p no:cacheprovider tests/test_downloader_runtime.py`: 61 passed.
- `python3 -m pytest -q -p no:cacheprovider tests/test_youtube_parser.py tests/test_main_urls.py tests/test_youtube_subscriptions.py`: 27 passed.
- `python3 -m pytest -q -p no:cacheprovider`: 158 passed, 1 existing Starlette template deprecation warning.

### Deploy Impact

- Included in `ghcr.io/mephiblin/hugcivi:sha-625957b` and `ghcr.io/mephiblin/hugcivi:latest`.

### Handoff Notes

- Operators can still force strict subtitle behavior by setting explicit yt-dlp subtitle/error options in `YT_DLP_EXTRA_OPTIONS`.

### Follow-Up

- None.

## Scoped Folder Search

Time: 01:51 KST
Commit: `16e640b`
Status: verified

### Summary

- Scoped the lower-left storage folder search to the currently selected folder and its descendants.
- Kept `/data` root and no-selection states as full loaded-tree search.
- Kept the move destination tree global so folder moves can still target locations outside the selected search scope.

### Code And Docs

- Backend: None.
- Frontend/addon: Added folder search scope helpers and scoped result filtering in `app/templates/index.html`.
- Tests: Extended `test_home_template_declares_storage_folder_search_ui` to cover scoped search helpers and unchanged move-tree row collection.
- Docs: Updated `README.md`, `PATCH_NOTES.md`, `docs/index.md`, `docs/feature-code-map.md`, and `docs/storage-folder-search-design-2026-07-02.md`.

### Verification

- `git diff --check`: passed.
- `python3 -m py_compile app/main.py app/db.py app/downloader.py app/internal_jobs.py app/parsers.py app/workflows.py app/metadata.py app/utils.py`: passed.
- `node --check chrome-extension/shared.js && node --check chrome-extension/background.js && node --check chrome-extension/popup.js && node -e "JSON.parse(require('fs').readFileSync('chrome-extension/manifest.json','utf8'))"`: passed.
- `python3 -m pytest -q -p no:cacheprovider tests/test_review_fixes.py`: 47 passed, 1 existing Starlette template deprecation warning.
- `python3 -m pytest -q -p no:cacheprovider`: 156 passed, 1 existing Starlette template deprecation warning.
- Playwright smoke on a temporary server with `alpha/shared` and `beta/shared`: `/data` search returned both folders, selecting `/data/alpha` narrowed the same query to `/data/alpha/shared`, and the home button restored `/data` full-scope results.

### Deploy Impact

- Included in `ghcr.io/mephiblin/hugcivi:sha-625957b` and `ghcr.io/mephiblin/hugcivi:latest`.

### Handoff Notes

- The search still only covers the currently loaded folder tree from `build_folder_tree(max_depth=4, max_entries=300)`.
- A deeper server-side search endpoint remains a separate future enhancement.

### Follow-Up

- None.
